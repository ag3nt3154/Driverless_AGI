# PROJECT_CONTEXT.md

> Last updated: 2026-06-28 | [README](README.md) | [TODO](TODO.md)
> Session 2026-06-28 (8): Telegram bot interface added. New `tg/` package (`bot.py`, `callbacks.py`, `session.py`, `utils.py`) + `telegram_bot.py` entry point. Uses `python-telegram-bot>=22.0` (async). Architecture: `run_in_executor` bridges async Telegram handlers to sync `AgentLoop` (Harbor pattern); `run_coroutine_threadsafe` bridges sync callbacks back to async Telegram sends. Per-chat `ChatSession` holds multi-turn `_messages` + `threading.Lock` for serialisation + `pending_ask` tuple for `ask_user` mid-task blocking. Package named `tg/` (not `telegram/`) to avoid shadowing `python-telegram-bot`'s own `telegram` module. `load_telegram_config()` added to `agent/config_loader.py`. Config: `telegram.bot_token_env` in config.yaml. Commands: `/start`, `/clear`, `/help`.
> Session 2026-06-28 (7): TODO intent detection added to `memory-add` subagent. New Step 0.5 in `.dagi/subagents/memory-add/prompt.md` detects first-person intention phrases ("I want to", "I plan to", "I need to", etc.) and routes them to `wiki/user-todo.md` as structured TODO entries (numbered `[TODO-NNN]`, with date added/due, status, description, proposed method, related nodes). When triggered, the subagent skips the normal knowledge-filing Steps 1–6 entirely. `/init` command in `cli.py` updated: `user-todo.md` added to wiki stubs (created on init with proper frontmatter), `.index.md` stub now includes User TODO row, and `log.md` operations list now includes `add-todo`. `subagent_config.yaml` description updated to mention TODO detection.
> Session 2026-06-28 (6): TUI sidebar cosmetic overhaul. Three columns now have equal ratio (1/1/1, was 2/4/3). Left column (`_status_col`) converted from `Table` to `Group(face_text, info_grid)` so the multi-line emote renders above the info grid without column-span constraints. `_path_tail(path, max_chars=36)` added to `tui/sidebar.py` — left-truncates paths with `…` prefix so cwd/app/mem rows show the deepest part of the path. Status and model name combined onto one info-grid row. All five emote files (`.dagi/emotes/*.md`) upgraded from single-line kaomoji to 5-line ASCII art with distinct border characters per emotion (╔/╚ for default/serious, ★ for happy, ┌/└ for confused, ≋ for funny).
> Session 2026-06-28 (5): `explore_files` subagent prompt redesigned with FastContext innovations. Output format changed from prose report (Summary / Key Files table / Findings / Recommendations) to citation-first format (Summary ≤80 words / Citations as `path:line_start-line_end` entries / Notes ≤5 bullets). Prompt now instructs broad-to-narrow search strategy and explicit parallel tool calls. Only `.dagi/subagents/explore_files/prompt.md` changed — handoff mechanism, subagent_config.yaml, and all infrastructure unchanged (context isolation was already implemented via subprocess IPC).
> Session 2026-06-28 (4): `plan-work-review` skill reordered — grill-me now runs as Step 2 (immediately after entering plan mode), before codebase exploration. Old Steps 2/3/4 renumbered to 3/4/5. Separate "Clarify" step removed (grill-me subsumes it). Exploration is now targeted: the agent knows exactly what to look for because requirements were stress-tested first. The DAGI `grill-me` skill at `.dagi/skills/grill-me/SKILL.md` was confirmed already present and adapted for DAGI (explore subagent, `ask_user` tool).
> Session 2026-06-28 (3): Scheduler interval unit changed from hours to seconds. `_MIN_HOURS` → `_MIN_SECONDS = 60.0`; `parse_interval(seconds)` validates ≥ 60 s; `ScheduledTask.interval: float` now stores seconds; `interval_td()` → `timedelta(seconds=self.interval)`. `schedule.example.yaml` updated (24.0 → 86400.0, 168.0 → 604800.0). `runner.py` stale `t.every` reference fixed → `t.interval`. `ListScheduledTasksTool` display unit updated (`h` → `s`). 40 unit tests, all passing. Two new `RunTracker` tests added for sub-minute precision.
> Session 2026-06-28 (2): Scheduler interval simplified to float-only (hours — later superseded by seconds in session 3). `parse_interval()` no longer accepts string aliases (`"daily"`, `"weekly"`, `"6h"`) — only `float | int`. `ScheduledTask.interval` field type changed from `str` to `float`; `interval_td()` uses `timedelta(hours=self.interval)`. `ScheduleTaskTool.run()` signature updated to `interval: float`; JSON schema property type changed to `"number"`. `.dagi/scheduler/schedule.example.yaml` updated (`"daily"` → `24.0`, `"weekly"` → `168.0`). `tests/test_scheduler.py` fully rewritten for float-only: string-alias parse tests removed, zero/negative/non-numeric tests kept, 4 new float-specific tests added. 35 unit tests, all passing.
> Session 2026-06-28: Task scheduler added. New `scheduler/` package: `models.py` (ScheduledTask dataclass, `parse_interval` — hours-based float intervals, min 1/60 h), `tracker.py` (RunTracker — append-only `runs.jsonl`, `is_due()` via timedelta comparison), `runner.py` (entry point `python -m scheduler.runner [--dry-run]`, runs due tasks sequentially via AgentLoop with `ask_user_timeout=60` and `plan_mode_initiated_by="dagi"` for full autonomy). `tools/schedule_tools.py`: three interactive-only tools (`schedule_task`, `list_scheduled_tasks`, `remove_scheduled_task`) registered only when `plan_mode_initiated_by == "user"` — autonomous tasks cannot self-modify the schedule. `run_scheduler.bat` is the Windows trigger (wire into Task Scheduler). `AgentConfig.ask_user_timeout: int | None` field added; `create_tool_registry` respects it for the normal-mode `AskUserTool`. `.dagi/scheduler/schedule.example.yaml` added as reference. 28 unit tests, all passing.
> Session 2026-06-27 (4): Memory-wiki system rework. BM25 retrieval removed entirely (`agent/memory_retriever.py` deleted, `.dagi/skills/memory-query/bm25_query.py` deleted). `memory-add` and `memory-query` converted from skills to proper subagents (`.dagi/subagents/memory-add/`, `.dagi/subagents/memory-query/`) with restricted file access (`root: memory_root` sentinel in config → cwd+allowed_roots locked to memory_root only). `build_subagent_registry()` updated with new `memory_root: Path | None` param handling the sentinel. `/init` command updated for new wiki structure: `projects/` + `knowledge/` sections, `.index.md` naming (Obsidian-compatible). Wiki index auto-injection added in `loop.py` (`_build_wiki_index_context()`). Memory Protocol section added to system prompt enforcing read-before/write-after discipline. `memory-ingest`, `memory-lint`, `memory-discuss` skills updated to match new structure. Old `memory-add` and `memory-query` skill directories deleted.
> Session 2026-06-27 (3): Extracted unified `_assemble_system_string(dagi_root) -> str` in `agent/loop.py`. All 8 system-prompt assembly steps (load prompt text, format-map substitutions, preamble, project root, active_plan_file block, `system_parts` refresh) now live in one method, called from `__init__`, `_rebuild_for_normal_mode`, and `_rebuild_for_plan_mode`. Eliminated 4 confirmed divergence-class bugs from future code paths. Bonus fix: `system_parts` is now refreshed on every rebuild (previously only in `__init__`), keeping the UI expander fresh after mode transitions. `_effective_memory_root` inline recomputation in both rebuild methods removed (was a known duplicate of `self._effective_memory_root`).
> Session 2026-06-27 (review): 2 new findings added to TODO.md; 0 items escalated; 0 completed entries archived. New finding: `pyproject.toml` declares `crawl4ai>=0.4` allowing CVE-affected versions (SSRF, auth bypass, path traversal — patched in 0.8.7). Also found `agent/prompts.py:5-6` still uses independent `Path(__file__)` computation (same class as the 4 prior DAGI_ROOT divergence bugs). All YAML loading uses `safe_load` — no PyYAML CVE exposure. GNHF self-review still dormant at 63 days.
> Session 2026-06-27 (2): Fixed three bugs from TODO. (1) Replaced 5 remaining independent `Path(__file__)` DAGI_ROOT computations in `cli.py` (×2), `tui/app.py`, `tui/commands.py`, and `tools/spawn_subagent.py` with `from agent import DAGI_ROOT`. (2) `agent/loop.py:547-562` now parses `tc.function.arguments` once (`args = json.loads(...)`) and reuses across all 4 dispatch branches — eliminates redundant parses and the uncaught `JSONDecodeError` risk on malformed JSON. (3) `agent/tools.py:_discover_subagent_tools` now scans both `DAGI_ROOT/.dagi/subagents` (built-in types) and `cwd/.dagi/subagents` (project types, override on name collision); cwd==DAGI_ROOT case deduplicates via identity check.
> Session 2026-06-27: Fixed three XS-effort bugs: (1) `_rebuild_for_reload` in `agent/loop.py:910` now derives `interactive` from `self.config.plan_mode_initiated_by == "user"` before calling `_rebuild_for_plan_mode` — prevents autonomous plan mode from silently flipping to interactive (infinite `ask_user` wait). (2) `_load_subagent_config` in `agent/tools.py:43` now tries `project_path` first, then `_DAGI_ROOT` fallback — `build_subagent_registry` no longer crashes with `ValueError` for non-DAGI projects. (3) `_handle_enter_plan_mode` scaffold in `agent/loop.py:631` now includes `"## Execution Protocol\n\n"` after `"## Verification\n\n"` — plan document is self-contained regardless of compaction timing.
> Session 2026-06-26 (8): Fixed both pipe-buffer deadlock bugs. (1) `_subagent_runner.py`: added `_drain_stdout()`, always start a drain thread regardless of `on_event` — `_stream_stdout` when relay is needed, `_drain_stdout` to devnull otherwise. Added `extra_argv` param to `run_subagent()`. (2) `cli_subagent.py`: replaced hand-rolled `Popen`+`proc.wait()` with `run_subagent()`, wired `on_event_factory` and `tracker`, moved prompt-file cleanup into `try/finally`, timeout now returns resumable PID dict. (3) `agent/tools.py`: passes `on_event_factory` and `tracker` when constructing `SpawnCliSubagentTool`. Custom subagents now appear in TUI, are tracked in session logs, and can be resumed after timeout.
> Session 2026-06-26 (7): Fixed `_handle_complete_plan` stale `Path(__file__).parent.parent` — the one call site missed by the `cdf90a8` centralisation commit. Replaced with `DAGI_ROOT` (already imported at line 13). `agent/loop.py:677`.
> Earlier sessions compressed 2026-06-27: 22 sessions from 2026-06-17 to 2026-06-26 (6) — see git log for full history.


---

## Project Description

Driverless AGI (dagi) is a self-hosted, OpenAI-compatible agentic coding assistant built entirely in Python. It takes a task from the user, runs a Plan→Act→Observe loop calling tools (read, write, edit, bash, grep, web search, etc.) until the task is complete, and surfaces results via a Rich interactive CLI or single-shot `main.py` entry point. See [README](README.md) for setup and usage.

## Objective / Problem Statement

Build a minimal but production-capable autonomous coding agent that can: work on arbitrary codebases, survive long tasks via context compaction, accumulate persistent knowledge via a wiki memory system, spawn specialist subagents for research/planning, and self-improve over time via the GNHF feedback loop.

Non-goals: cloud hosting, multi-user auth, UI beyond CLI/Rich.

## Environment

- **Language:** Python 3.11+ (conda env `dagi` currently runs 3.14 pre-release — see Notable Points)
- **Runtime:** `conda` environment named `dagi`
- **Install:** `conda run -n dagi pip install -e .`
- **Run (REPL):** `conda run -n dagi python cli.py`
- **Run (TUI):** `conda run -n dagi python tui.py`
- **Config:** `config.yaml` (gitignored) — model catalog, base_url, api_key / api_key_env, max_iterations, null_response_retries, max_continuations, cache_prompt, tools

## Directory Layout

```
Driverless_AGI/
├── agent/              # Core engine — loop, registry, config, session tracking
├── tools/              # Built-in tools (compact, spawn_subagent, extend_timeout, emote, etc.)
├── scripts/            # Utility scripts (dagi_freeze, build_api_tools, etc.)
├── tui/                # Textual TUI package (app, commands, sidebar, callbacks, etc.)
├── tg/                 # Telegram bot package (bot, callbacks, session, utils)
├── benchmarks/         # Benchmark adapters
│   ├── terminal_bench/ # Terminal-bench 2 adapter (TmuxBashTool, DagiAgent)
│   └── harbor/         # Harbor Framework adapter (HarborBashTool, DagiAgent)
├── scheduler/          # Task scheduler package (models, tracker, runner)
├── tests/              # Unit tests
├── .dagi/
│   ├── agents.md       # Behavioral guidelines loaded at every session start
│   ├── prompts/
│   │   ├── main/       # main_system.md — primary agent system prompt
│   │   ├── subagents/  # Per-type prompts (explore_files, web_research, worker, review)
│   │   └── compact/    # compact_system + compact_user prompts
│   ├── skills/         # Skills (memory-*, grill-me, update-project-context, etc.)
│   ├── subagents/      # Per-type subagent_config.yaml (tools list + model_tier)
│   ├── handoffs/       # Subagent handoff documents (generated at runtime)
│   ├── plans/          # Generated plan documents
│   ├── scheduler/      # schedule.yaml (task defs) + runs.jsonl (execution log) + output/
│   ├── logs/           # Session JSONL logs
│   └── self-review/    # Session review reports
├── .dagi/prompts/soul.md # Agent persona / identity
├── tui.py              # TUI entry point (thin launcher)
├── telegram_bot.py     # Telegram bot entry point (thin launcher)
├── cli.py              # Rich REPL entry point
├── config.yaml         # Runtime config (gitignored)
├── benchmarks/config_benchmark.yaml # Benchmark-specific config (moved here from project root)
├── docs/
│   └── terminal-bench.md # Guide for running Terminal-bench 2
└── README.md           # Full documentation
```

## Architecture

```
tui.py / cli.py / main.py / telegram_bot.py ← entry points (TUI | REPL | one-shot | Telegram)
    │
    tui.py → tui/ package:
        tui/app.py           ← DagiApp (lifecycle, threading)
        tui/commands.py      ← SlashCommandsMixin (_handle_slash + _cmd_*)
        tui/callbacks.py     ← build_callbacks() free function
        tui/conversation.py  ← ConversationPane(RichLog)
        tui/sidebar.py       ← Sidebar(Widget) — top header, 3-column layout
        tui/prompt_input.py  ← PromptInput(TextArea)
        tui/utils.py         ← helpers + _Stats
    │
    └── AgentLoop (agent/loop.py)
            │
            ├── ToolRegistry (agent/registry.py)  ← dispatches tool calls
            ├── SessionTracker (agent/session.py)  ← logs all turns to JSONL
            ├── CompactTool (tools/compact.py)     ← Pi-style context compaction
            ├── SkillLoader (.dagi/skills/)         ← skill guidance docs
            └── AgentCallbacks                     ← rendering hooks (TUI/CLI)
```

**Config:** `config.yaml` → `agent/config_loader.py` → `AgentConfig` dataclass  
**Memory:** `dagi-memory/{raw,wiki,sources}/` + wiki index auto-injection at session start; retrieval via `spawn_memory_query_subagent` (grep + traversal); writing via `spawn_memory_add_subagent`. Wiki is structured as `wiki/projects/{name}/` (project knowledge) and `wiki/knowledge/{topic}/` (general knowledge), with `.index.md` files at each level.  
**Subagents:** Pipe-based subprocess spawning (`tools/_subagent_runner.py`). Each subagent type has a `.dagi/subagents/<type>/subagent_config.yaml` (explicit `tools:` list + `model_tier`). Handoff path generated by `SpawnSubagentTool`, written by subagent on exit. Subagent stdout (newline-delimited JSON events) relayed to main TUI `ConversationPane` with `[subagent-type]` label prefix.  
**TUI rendering bridge:** `AgentCallbacks` fire on agent thread → `App.call_from_thread()` → Textual main loop → widget `refresh()`

## Process Flow

1. User calls `cli.py` (REPL) or `main.py` (one-shot) with a task string
2. `resolve_model_config()` reads `config.yaml`, resolves API key (direct or via env var), builds `AgentConfig`
3. `AgentLoop.__init__()` loads skills, builds `ToolRegistry`, constructs system prompt
4. `AgentLoop.run(task)` enters `while True` loop:
   - Checks `_pause_event` at top of each iteration — blocks if user pressed ESC (TUI only)
   - Calls the LLM with current `_messages`
   - If tool calls present → dispatch each tool, append results, loop again
   - If no tool calls → check response for termination flags:
     - `<<END_OF_RESPONSE>>` → strip flag, surface response, exit loop (wait for next user turn)
     - `<<TASK_END>>` → strip flag, surface response, call `on_done`, return (legacy alias)
     - Neither → inject continue prompt, loop again (up to `max_continuations`)
5. Context compaction triggers mid-loop if token count exceeds threshold
6. Session ends; `SessionTracker.finish()` writes summary to `.dagi/logs/`

## Key Files & Directories

| Path | Purpose |
|------|---------|
| `agent/loop.py` | Core agent loop, `AgentConfig`, `AgentCallbacks`, `TASK_END_FLAG`, `AWAIT_USER_FLAG`; pause/resume via `_pause_event`; `_extra_body` dict for OpenRouter extensions (reasoning, cache_prompt, provider routing); `_assemble_system_string(dagi_root)` is the single source of truth for all system-prompt assembly — called from `__init__`, `_rebuild_for_normal_mode`, and `_rebuild_for_plan_mode`; `_build_wiki_index_context(memory_root)` reads `.index.md` files and returns a `[WIKI INDEX]...[END WIKI INDEX]` block injected as a system message before the first user turn |
| `agent/config_loader.py` | Reads `config.yaml`; resolves `api_key` (direct or env var) and model catalog. `resolve_model_config(project_path=...)` merges `{project_path}/.dagi/config.yaml` over root; `_load_project_config` / `_merge_configs` are the two helper functions. |
| `agent/tools.py` | Wires all tools into `ToolRegistry`; defines plan-mode and subagent registry variants; `build_subagent_registry()` accepts `memory_root: Path \| None` — when a subagent's `subagent_config.yaml` declares `root: memory_root`, the registry restricts all file tools' `cwd` and `allowed_roots` to that path only |
| `.dagi/subagents/memory-query/` | Subagent for wiki retrieval: grep-based + index traversal; writes handoff file with citations; restricted to memory_root via `root: memory_root` sentinel |
| `.dagi/subagents/memory-add/` | Subagent for wiki writes: Step 0.5 detects TODO intent (first-person plans/goals) and appends to `wiki/user-todo.md`; otherwise classifies project vs. general knowledge, writes 5-field frontmatter nodes, updates `.index.md` files, appends to `log.md`; restricted to memory_root |
| `scheduler/models.py` | `ScheduledTask` dataclass, `parse_interval()` (hours float→timedelta), `load_schedule()` / `save_schedule()` |
| `scheduler/tracker.py` | `RunTracker` — loads `runs.jsonl` at init, `is_due(task)`, `record_run(...)` |
| `scheduler/runner.py` | Entry point (`python -m scheduler.runner`); runs due tasks via `AgentLoop` with autonomous config |
| `tools/schedule_tools.py` | `ScheduleTaskTool`, `ListScheduledTasksTool`, `RemoveScheduledTaskTool` — interactive sessions only |
| `run_scheduler.bat` | Windows trigger; wire into Task Scheduler |
| `.dagi/scheduler/schedule.yaml` | Task definitions (created by user or `schedule_task` tool) |
| `.dagi/scheduler/runs.jsonl` | Execution log (append-only); read by `RunTracker` and `list_scheduled_tasks` |
| `agent/registry.py` | Tool dispatch; OpenAI function-schema generation |
| `agent/session.py` | Append-only JSONL session logging with token/cost tracking |
| `tools/_subagent_runner.py` | Core pipe-based subagent runner; `run_subagent()` + `resume_subagent()`; `_active` dict keyed by PID |
| `tools/extend_timeout.py` | `ExtendSubagentTimeoutTool` — extends deadline on in-flight subagent via `resume_subagent(pid, extra_seconds)` |
| `tools/spawn_subagent.py` | `SpawnSubagentTool` — generates handoff path, calls `run_subagent()`, returns path to parent |
| `tools/compact.py` | Pi-style context compaction |
| `tools/ask_user.py` | Blocking user-input tool with optional timeout |
| `telegram_bot.py` | Thin launcher — loads `.env`, reads `load_telegram_config()`, imports `TelegramBot` from `tg/`, defines typer entry point |
| `tg/bot.py` | `TelegramBot` class — handlers (`_cmd_start`, `_cmd_clear`, `_cmd_help`, `_handle_message`), `_run_agent_task()` async/sync bridge via `run_in_executor`. Captures event loop in `_post_init` for callback bridging |
| `tg/callbacks.py` | `build_callbacks(bot, chat_id, session, event_loop) → AgentCallbacks` — bridges sync callbacks to async Telegram sends via `run_coroutine_threadsafe`. `on_ask_user` uses `threading.Event` + container list (same pattern as `tui/callbacks.py`) |
| `tg/session.py` | `ChatSession` dataclass (per-chat state: messages, lock, pending_ask) + `SessionManager` (dict[chat_id, ChatSession]) |
| `tg/utils.py` | `chunk_message(text, limit=4096)` splits on paragraph boundaries; `format_ask_options()` formats numbered options |
| `tui.py` | Thin launcher (~30 lines) — loads `.env`, imports `DagiApp` from `tui/`, defines typer entry point |
| `tui/app.py` | `DagiApp(SlashCommandsMixin, App[None])` — lifecycle only: compose, on_mount, dispatch, callbacks wiring, poll; ~180 lines |
| `tui/commands.py` | `SlashCommandsMixin` — `_handle_slash` + all `_cmd_*` methods + `_load_maps`; mixed into `DagiApp` via MRO |
| `tui/callbacks.py` | `build_callbacks(app, loop_ref) → AgentCallbacks` free function; uses `TYPE_CHECKING` guard to avoid circular import |
| `tui/conversation.py` | `ConversationPane(RichLog, wrap=True)` — scrollable Rich log widget |
| `tui/sidebar.py` | `Sidebar(Widget)` — fixed 6-line top header; 3-column Rich `Table`: left=emote+status+model, center=tokens+condensed-context, right=plan subtasks; 2 s plan-poll via `set_interval` in `DagiApp.on_mount` |
| `tui/prompt_input.py` | `PromptInput(TextArea)` + inner `Submitted` message |
| `tui/utils.py` | `_colour`, `_truncate`, `_resolve_option`, `_breakdown`, `_system_breakdown`, `_Stats`, `_TOOL_COLOURS`, `_SLASH_HELP` |
| `cli.py` | Rich REPL with threaded/sync modes, plan mode, slash commands |
| `config.yaml` | Model catalog, API config, context window settings |
| `.dagi/prompts/main/main_system.md` | Agent system prompt (tools, termination flags, plan mode trigger) |
| `tools/_plan_parser.py` | Utilities for parsing plan.md: `extract_global_sections`, `extract_subtask`, `parse_subtask_statuses` |
| `.dagi/agents.md` | Behavioral guidelines, Plan-Work-Review cycle instructions |
| `.dagi/prompts/soul.md` | DAGI persona definition (moved from `soul.md` at repo root) |
| `agent/prompts.py` | Prompt loaders: `load_prompt`, `load_subagent_prompt`, `load_main_system_prompt`, `load_soul`. The latter two resolve project-local `.dagi/prompts/` first, then fall back to dagi root. |
| `tests/test_continuation.py` | Unit tests for `<<TASK_END>>`, `<<WAIT_FOR_USER_RESPONSE>>` loop logic, and transient API error retry |
| `tests/test_project_config.py` | Tests for `load_main_system_prompt`, `load_soul` prompt resolution helpers (5 tests) and `resolve_model_config` project config merge (4 tests, 9 total). |
| `tests/conftest.py` | RAM watchdog pytest plugin — auto-use fixture monitors system RAM during every test; fails at 70%, hard-kills at 90% |
| `tests/test_config_loader.py` | Unit tests for direct `api_key` and `api_key_env` resolution |
| `requirements.txt` | Exact pip freeze of the `dagi` conda env (23 packages). **Does not match `pyproject.toml`** — see Notable Points. |
| `tools/emote.py` | `EmoteTool(BaseTool)` — agent calls `emote(emote)` to update sidebar face; fires `on_emote` callback |
| `.dagi/emotes/` | Five emote face files (`default`, `confused`, `happy`, `serious`, `funny`), one line each, plain text |
| `tools/tmux_bash.py` | `TmuxBashTool(BaseTool)` — first-class tmux-based bash tool (`name="tmux_bash"`); canonical location; routes commands through Terminal-bench's `TmuxSession` |
| `benchmarks/terminal_bench/tmux_bash_tool.py` | Legacy location for `TmuxBashTool` — kept for backwards compatibility; `benchmarks/terminal_bench/agent.py` now imports from `tools.tmux_bash` |
| `benchmarks/terminal_bench/agent.py` | `DagiAgent(BaseAgent)` — Terminal-bench 2 entry point; instantiates `TmuxBashTool(session)` from `tools.tmux_bash` and passes it to `AgentLoop(_bash_tool=bash_tool)` |
| `benchmarks/harbor/bash_tool.py` | `HarborBashTool(BaseTool)` — Harbor bash tool (`name="harbor_bash"`); wraps a sync `exec_fn` callable provided by `DagiAgent` that bridges `environment.exec()` from the event loop |
| `benchmarks/harbor/agent.py` | `DagiAgent(BaseAgent)` — Harbor Framework entry point; runs `AgentLoop` in `run_in_executor` thread, bridges async `environment.exec()` back via `run_coroutine_threadsafe` |
| `benchmarks/config_benchmark.yaml` | Benchmark-specific config: `bash_backend: subprocess` (no-op), raised `max_continuations`, explicit `tools:` list with `harbor_bash` (not `bash`) for Harbor container access; used by both Terminal-bench and Harbor adapters |
| `benchmarks/run_terminal_bench.bat` | Windows runner: sets `DAGI_BENCH_MODEL`, calls `tb run --agent-import-path benchmarks.terminal_bench.agent:DagiAgent` |
| `benchmarks/run_harbor.bat` | Windows runner: sets `DAGI_BENCH_MODEL` + `HARBOR_DATASET`, calls `harbor run --agent-import-path benchmarks.harbor.agent:DagiAgent` |
| `docs/terminal-bench.md` | User guide: prerequisites, one-time setup, model selection, smoke test, full run, result interpretation |

## Encountered Errors & Solutions

- **2026-05-29 Error**: `api_key` field in config.yaml silently ignored; dagi fell back to OpenAI env vars causing auth errors when `OPENAI_API_KEY` was unset.
  **Cause**: `_build_config_from_entry()` only read `api_key_env` (env var pointer), never a direct `api_key` literal.
  **Fix**: Added direct-key check first (`entry.get("api_key", "")`); falls back to `api_key_env` path only when empty. Warning block in `resolve_model_config()` similarly skips warning when direct key present.

- **2026-05-29 Error**: DAGI did not return `<<WAIT_FOR_USER_RESPONSE>>` on conversational/greeting responses, causing the harness to inject "continue" and confuse the model.
  **Cause**: System prompt listed `<<WAIT_FOR_USER_RESPONSE>>` examples only as "clarifying questions / options / intermediate results" — the model's pattern-matching excluded greetings and casual replies. The instruction was present but under-specified.
  **Fix**: Rewrote the flag section in `.dagi/prompts/main/main_system.md` to make the rule unconditional — every no-tool-call response must carry a flag; `<<WAIT_FOR_USER_RESPONSE>>` is now explicitly listed as the catch-all including greetings and conversational turns.

- **2026-05-29 Error**: Agent responses that ask a question or surface intermediate results immediately triggered auto-continue injection, preventing genuine conversational turns.
  **Cause**: Only two exit conditions existed — `<<TASK_END>>` (done) and no flag (inject "continue").
  **Fix**: Added `<<WAIT_FOR_USER_RESPONSE>>` as a third flag: exits the loop cleanly like `<<TASK_END>>` but semantically signals "waiting for user reply." CLI multi-turn history is preserved across `run()` calls, so the conversation continues naturally.

- **2026-05-30 Note**: `conda run -n dagi python -c "..."` with multi-line strings fails on Windows with `NotImplementedError` (newlines in args not supported). Write multi-line scripts to a temp file and run that instead.

- **2026-05-31 Change**: Renamed `<<AWAIT_USER_RESPONSE>>` → `<<END_OF_RESPONSE>>`.
  **Cause**: Models kept omitting the flag on conversational turns (greetings, etc.) because `AWAIT_USER_RESPONSE` reads as something said only after asking a question. The instruction was also too soft.
  **Fix**: Renamed the flag to `<<END_OF_RESPONSE>>` (semantically: "I am done with my turn") and rewrote the system-prompt instruction block with examples, explicit consequences, and a bold header. Constant renamed in `agent/loop.py`; tests were unaffected (they import the constant).

- **2026-05-31 Bug**: `_continuation_count` never reset between `run()` calls — PROJECT_CONTEXT.md incorrectly stated it reset per task; the code did not.
  **Cause**: `_continuation_count = 0` was set only in `__init__`, never at the start of `run()`. Accumulated across all tasks in a multi-turn session, silently eroding the continuation budget.
  **Fix**: Added `self._continuation_count = 0` at the top of `run()` (after `tracker.record_user`). Updated notable point to reflect corrected behavior.

- **2026-05-31 Bug**: Ghost-response cascade — DAGI silently ran 10 continuation loops and went idle after a null API response.
  **Cause**: DeepSeek v4 Flash via OpenRouter returned HTTP 200 with `content=None`, no tool calls, and `usage=None`. The loop appended `{"role":"assistant","content":null}` to `_messages`, causing every subsequent call to also return empty (malformed history triggered consistent null responses). The outer loop consumed all 10 `max_continuations` slots silently.
  **Fix**: Wrapped the API call in an inner ghost-response retry loop. Ghost = `content=None` AND `prompt_tokens==0`. On ghost: discard the response, do NOT append to `_messages`, retry up to `null_response_retries` (default 3, configurable in `config.yaml`). After all retries fail: surface "Error: model returned null N times" in TUI and return — user sees the error and can retry.

- **2026-05-31 Bug**: `plan_mode_exited` field was dead code — initialized `False` in `__init__`, never set `True` anywhere. The check at the end of the tool-call loop could never trigger.
  **Cause**: `_handle_exit_plan_mode` set `self.exited_plan_file` (a different field) rather than `self.plan_mode_exited`. Plan mode exit worked incidentally via the extra API round-trip.
  **Fix**: Removed the `plan_mode_exited` field and its unreachable check block. Accepted the natural flow (extra API call, agent reads plan context and starts implementing) as canonical.

- **2026-05-31 Feature**: ESC pause button added to TUI. `AgentLoop` now has `_pause_event: threading.Event` (set = running), checked at the top of each `while True` iteration. `pause()` clears the event; `inject_and_resume(message)` appends the user message to `_messages` then sets the event. TUI wires ESC to `action_pause()` and re-enables input; next typed message calls `inject_and_resume()` to unblock the loop. This is semantically equivalent to a user-initiated `ask_user` without a question.

- **2026-05-31 Refactor**: Unified skill invocation entry points. Previously, user slash commands (`/plan-work-review`, etc.) eagerly loaded full skill content into the user message via `_inject_skill_content()` (cli.py) / `_inject_skill()` (tui.py), bypassing the `skill` tool entirely. Now, slash commands produce a plain instruction (`"Invoke the \`skill-name\` skill."`) and the LLM calls `skill()` itself — identical to mid-task internal invocations. Removed both helper functions; added `_skill_invocation_message()` in cli.py as the single shared formatter, imported by tui.py.

## Notable Points

- **Flag ordering matters**: `AWAIT_USER_FLAG` (`<<END_OF_RESPONSE>>`) is checked *before* `TASK_END_FLAG` in the loop. If both appear in a response (accidental), `<<END_OF_RESPONSE>>` wins.
- **`api_key` vs `api_key_env`**: Direct `api_key` in config.yaml overrides env var lookup. Empty string `""` still falls through to env var — only a truthy value short-circuits. Security note: putting the key in yaml means it could be committed; prefer `api_key_env` for production use.
- **Multi-turn message history**: The CLI passes `loop._messages` as `conversation_msgs` into the next `_run_task()` call. `<<WAIT_FOR_USER_RESPONSE>>` works without any CLI changes because of this existing design.
- **`null_response_retries` is per-API-call** — the inner retry loop resets for every new LLM call. A ghost response during tool-call processing (e.g. after a large skill result) retries that specific call up to N times. This is independent of `max_continuations`.
- **`supports_pause` gates error-pause behavior**: `AgentCallbacks.supports_pause` (default `False`) controls whether exhausted transient retries pause the loop (TUI path) or raise (CLI/subagent path). The TUI's `build_callbacks()` sets it `True`. This explicit boolean is intentional — checking `on_pause is not lambda: None` would be fragile since Python lambdas aren't singletons.
- **`_active_loop` now set before `loop.run()`**: In `tui/app.py._agent_work()`, `self._active_loop = loop` is assigned immediately after `AgentLoop.__init__()` completes, before `loop.run()` is called. This means even non-transient raises (401, 403) preserve the loop's `_messages` for the next task's `initial_messages`. Previously, any exception skipped the assignment entirely.
- **`_extra_body` unifies all OpenRouter extensions**: `AgentLoop.__init__()` builds `self._extra_body: dict` by combining the `reasoning` effort dict (when `thinking != "none"`), `cache_prompt: True` (when `config.cache_prompt` is set), and `provider: {order: [...]}` (when `config.provider_order` is set). `_handle_switch_model` rebuilds `_extra_body` from `self.config` after each tier switch — so `self.config.provider_order` must be set to the target tier's value *before* the rebuild, which is now done correctly (fixed 2026-06-21). To add a new provider extension, add a field to `AgentConfig`, read it in `config_loader._build_config_from_entry`, add it to `_base_config_snapshot`, restore it in the `"default"` branch, copy it in the `tier_cfg is not None` branch, and merge it into `_extra_body` in `_handle_switch_model`.
- **`provider_order` is per-model, not top-level**: `AgentConfig.provider_order: list[str] | None` is read from the per-model catalog entry (not the top-level `raw` config) because provider routing is model-specific — you're choosing which backend serves a particular model endpoint. Example: `provider_order: [Anthropic, Together]` on a claude-sonnet entry routes to Anthropic first, Together as fallback. Slugs are OpenRouter's internal provider identifiers. Ignored entirely by non-OpenRouter endpoints (the extra_body field is silently dropped). Added 2026-06-17.
- **`api_error_retries` is per-iteration** — the transient error retry counter (`_error_retries`) resets at the start of each loop iteration, giving every API call a fresh retry budget. Transient codes (429, 500, 502, 503) and connection/timeout errors are retried with exponential backoff (`2^attempt` seconds, capped at 60s). Non-transient errors (e.g. 401, 403) propagate immediately. This is independent of both `null_response_retries` and `max_continuations`.
- **`max_continuations` is per-`run()` call** — `_continuation_count` resets to 0 at the start of each `run()` call, so every task gets a fresh budget of `max_continuations` (default 10).
- **Subagents use pipe-based IPC**: `subprocess.Popen(stdout=PIPE, stderr=STDOUT)` — cross-platform, no separate window. A daemon thread reads each stdout line as a newline-delimited JSON event and relays it via `on_subagent_event_factory` in `AgentCallbacks` → `build_subagent_relay_callback()` in `tui/callbacks.py` → `ConversationPane` with `[bold cyan][{subagent_type}][/bold cyan]` prefix.
- **Handoff path is owned by the tool, not the parent**: `SpawnSubagentTool.run()` generates `.dagi/handoffs/{type}_{uuid8}.md` and passes it as `--handoff` to the child CLI. The parent agent never specifies or knows the path in advance — it receives it only after the subagent finishes as the tool result string, then calls `read_file(path)` to reason about it.
- **`subagent_config.yaml` replaces `config.yaml`**: each subagent folder (`.dagi/subagents/<type>/`) now contains `subagent_config.yaml` with an explicit `tools:` list. `_tools_from_list()` in `agent/tools.py` maps name strings to tool instances. No scope presets remain.
- **`extend_subagent_timeout` is the resume path**: when `SpawnSubagentTool` times out, it returns `{"status": "timeout", "pid": <pid>}` and exits the tool call. The subprocess is still alive in `_active[pid]`. The agent then calls `extend_subagent_timeout(pid, extra_seconds)` to resume polling without re-spawning.
- **`on_subagent_event_factory`**: a new field on `AgentCallbacks` — a factory that takes `subagent_type: str` and returns a `Callable[[str], None]` relay callback. The factory is called once at subagent spawn time; the returned callback is invoked for each stdout line from the child process.
- **`pyproject.toml` is incomplete**: `typer`, `rich`, and now `textual` are missing from declared deps. `pip install -e .` will fail on a clean environment for CLI/TUI use.
- **TUI thread safety**: `tui/app.py` runs `AgentLoop` on a daemon `threading.Thread`. All widget mutations from callbacks use `App.call_from_thread()`. The Sidebar widget uses instance attributes + `self.refresh()` — NOT Textual `reactive` — because Textual's reactive equality check on dicts can miss updates when the dict reference changes but content matches. Always use `dict(buckets)` (copy) when updating `_buckets` to force reference inequality.
- **Sidebar is now a top header, not a right column**: `Sidebar` is yielded first in `DagiApp.compose()` with CSS `height: 12; border-bottom: solid $panel`. `Sidebar.render()` returns a 3-column `rich.table.Table(expand=True, box=None)` with equal `ratio=1` columns: left=emote+paths (`_status_col()`), center=token+context (`_tokens_context_col()`), right=plan (`_plan_col()`). `_status_col()` returns a `rich.console.Group(face_text, info_grid)` — the multi-line emote `Text` is rendered above the info `Table.grid` without needing column spans. Paths (cwd, app, mem) are displayed via `_path_tail(path, max_chars=36)` which left-truncates with `…` so the deepest path component is always visible.
- **`SlashCommandsMixin` accesses `DagiApp` state freely**: `tui/commands.py` defines `SlashCommandsMixin` with no `__init__`. Its methods reference `self._active_loop`, `self._project_path`, `self.query_one(...)`, etc. — these resolve to `DagiApp` instance attributes at runtime via Python's MRO. Adding new instance attributes in the mixin requires no boilerplate; they must be initialized in `DagiApp.__init__` as usual.
- **`build_callbacks` circular import guard**: `tui/callbacks.py` imports `DagiApp` only inside `if TYPE_CHECKING:` to enable the type annotation `app: DagiApp`. At runtime no import of `tui/app.py` occurs from `callbacks.py`, breaking the cycle. The function accesses `app._stats`, `app._verbose`, `app.query_one(...)`, and `app.call_from_thread(...)` — all resolved dynamically.
- **TUI pause state**: ESC pauses at end-of-iteration (safe checkpoint). `_current_loop_ref: list` on `DagiApp` holds the running loop reference during execution (populated once `AgentLoop` is constructed in the worker thread). `action_pause()` guards against: idle (no live worker), pending `ask_user`, already paused. Resume requires explicit user input — no silent resume. The input branch in `on_prompt_input_submitted` detects paused state via `not loop._pause_event.is_set()` and routes to `inject_and_resume()` rather than spawning a new `AgentLoop`.
- **`PromptInput` replaces `Input`**: The TUI input widget is now `PromptInput(TextArea)` — a custom subclass that intercepts `enter` (submit, clears via `load_text("")`) and `shift+enter` (insert `"\n"` for multi-line composition). `TextArea` has no `.placeholder` attribute — the `_show_ask_user` and pending-ask branches no longer set a placeholder. The message handler is `on_prompt_input_submitted` (Textual routing: `on_{class_snake}_{message_snake}`).
- **Plan subtask status format**: `### Subtask N: [marker] name` in plan.md headings. Four valid markers: `[ ]` pending, `[~]` in-progress, `[x]` complete, `[!]` failed. The plan skeleton pre-populates `[ ]` at `enter_plan_mode` time. `parse_subtask_statuses()` in `tools/_plan_parser.py` parses these.
- **`[~]` orphan semantics**: The plan-work-review skill marks a subtask `[~]` before spawning a worker subagent. If the loop is interrupted mid-execution, `[~]` persists in plan.md indefinitely — no auto-reset. On session resume, the user or agent must inspect the subtask and decide whether to retry or mark complete. The TUI sidebar displays `[~]` in amber as "interrupted."
- **TUI plan panel poll**: `DagiApp` runs a 2 s `set_interval` poll (`_poll_plan`) that reads `loop.config.plan_file or loop.config.active_plan_file` on the Textual main thread. Both attributes are plain strings set once by the agent thread; Python's GIL makes the read safe without an explicit lock. The poll is a no-op when no loop is active.
- **TUI scrolling**: `RichLog.auto_scroll = True` by default. Textual pauses auto-scroll when the user scrolls up and resumes when they reach the bottom — this is built-in behaviour requiring no custom scroll handling.
- **`agents.md` is now behavior-only**: Project Description, Objectives, Directory Structure, Environment, Known Issues, and Recent Changes sections were stripped from `.dagi/agents.md`. Those were either stale duplicates of PROJECT_CONTEXT.md or missing from it. `agents.md` now contains only Coding Standards, Behavioral Guidelines, Memory protocol, and Error handling — 74 lines, no project state. The file now opens with a pointer to PROJECT_CONTEXT.md for all project information.
- **`update-project-context` skill added to DAGI**: `.dagi/skills/update-project-context/SKILL.md` created. The skill is adapted from the global `~/.claude/skills/update-project-context/` with one DAGI-specific addition: the guideline that behavioral guidelines from `agents.md` must never be duplicated into PROJECT_CONTEXT.md. The skill is auto-discovered via `{tools_and_skills}` injection and available to the agent without any other wiring.
- **System prompt instructs automatic PROJECT_CONTEXT.md updates**: `.dagi/prompts/main/main_system.md` now has a `## Project Context` section that instructs the agent to call `skill("update-project-context")` before writing its final response on any task that changes the codebase, introduces tools/skills, resolves an error, or reveals non-obvious architecture. Skip rule: conversational turns and factual questions.
- **`grill-me` skill is now universal**: `.dagi/skills/grill-me/SKILL.md` was upgraded from a 13-line imperative prompt to a full ~200-line protocol. It now has three phases: Phase 1 silently gathers context (codebase, PROJECT_CONTEXT.md, memory-wiki via `skill("memory-query")`, provided paths, web); Phase 2 routes to Mode A (decision-tree interrogation for plans/ideas) or Mode B (Socratic questioning for topics/documents/codebases); Phase 3 writes a closing summary and records insights to PROJECT_CONTEXT.md (`skill("update-project-context")`) and memory-wiki (`skill("memory-add")`) without asking permission. The global `~/.claude/skills/grill-my-plan/` directory was renamed to `grill-me/` and holds identical content for Claude Code sessions.
- **Skill slash commands are LLM-delegated**: `/skill-name [args]` no longer pre-injects skill content. It produces `"Invoke the \`skill-name\` skill.\n\n{args}"` and sends it as the user message. The agent must call `skill("skill-name")` to load the content — same path as mid-task internal invocations. This means one extra LLM round-trip per slash command, but both code paths are now unified through `SkillTool`.
- **Sidebar emote system**: `Sidebar._load_face()` reads `.dagi/emotes/{_emote}.md` on every `refresh()` and returns the full file content (`.strip()`). Falls back to `(◉ ᴗ ◉)` on `OSError`. Emote files are now 5-line multi-line ASCII art; `.strip()` preserves internal newlines so `Text.from_markup(face)` renders as a multi-line block above the info grid. The five emotes use distinct border characters: `╔═╚` (default/serious), `★─` (happy), `┌─┘` (confused), `≋─` (funny). The agent switches emotes via `EmoteTool` → `AgentCallbacks.on_emote` → `App.call_from_thread(sidebar.update_emote, emote)`. Emote files are live-editable. To suppress the tool, exclude `emote` from the `tools:` list.
- **ANSI `[blue]` renders as purple**: Rich's `[blue]` maps to ANSI color 4, which most modern terminal emulators render as violet/purple (inherited from CGA). Use hex colors (`[#4da6ff]`) or `[bright_blue]` (ANSI 12) for a perceptually blue result. This bit the sidebar logo — all color markup now uses hex.
- **`explore_files` prompt uses citation-first output (FastContext innovation)**: As of 2026-06-28, the subagent's handoff file structure changed from a prose-heavy report (Summary / Key Files table / Findings / Recommendations) to a citation-first format: `## Summary` (≤80 words) + `## Citations` (`path:line_start-line_end — description` per line) + `## Notes` (≤5 bullets). The prompt instructs broad-to-narrow search strategy (glob/grep first, targeted reads second) and explicit parallel tool calls within a single turn. Context isolation was already implemented (subprocess IPC); only the output format changed. The `##  Key Files` table and `## Recommendations` sections are removed — the citations block replaces both. Inspired by the FastContext paper (microsoft/fastcontext) which showed citation-only output reduces main-agent token consumption by 60%+ while improving benchmark scores.
- **Memory subagents use `task` as the single parameter name**: `SpawnSubagentTool._compose_task()` has a generic fallback `return kwargs.get("task", "")` for non-worker/review/explore_files types. Both `memory-query` and `memory-add` subagent configs declare a single `task:` parameter to hit this fallback. Using any other name (e.g. `query`, `content`) would produce an empty task string — the subagent would receive nothing. Do not rename this parameter without also updating `_compose_task()` in `tools/spawn_subagent.py`.
- **Wiki index injection happens once, outside the iteration loop**: `_build_wiki_index_context()` is called in `AgentLoop.run()` immediately before `self._messages.append({"role": "user", ...})` — a single unconditional injection, not gated on `iteration == 1`. This is intentional: `run()` is called fresh per task, so the injection naturally occurs once per task without a loop counter check.
- **`root: memory_root` is a generic sentinel, not a hardcoded type list**: Any subagent config declaring `root: memory_root` gets memory-root-restricted file access. The mechanism is open for future memory-adjacent subagents — add the field to their config and they automatically get isolation. There is no allowlist of specific subagent type names in `build_subagent_registry()`.
- **Wiki structure uses `.index.md` (dot-prefixed)**: All navigation index files are named `.index.md`, not `index.md`. This is required for Obsidian compatibility — Obsidian treats files starting with `.` as special and does not show them in the main note list, letting them serve as folder navigation artifacts without cluttering the note browser. The `/init` command creates `.index.md` stubs in `wiki/`, `wiki/projects/`, and `wiki/knowledge/`.
- **Plan mode revision loop**: After writing a plan, the agent is instructed (via `main_system.md`) to call `show_plan` before `exit_plan_mode`. `show_plan` shows the plan, asks "Do you have any modifications?", and returns either "Plan approved — call `exit_plan_mode`" or "Modifications requested — revise and call `show_plan` again." The revision cycle repeats until approval. After `exit_plan_mode`, the agent outputs one implementation-start sentence and immediately begins tool calls without waiting for user confirmation.
- **Execution Protocol is in the plan document, not conversation context**: The plan-work-review skill writes an `## Execution Protocol` section into every plan.md. This section contains the worker/review subagent delegation instructions. Because it's on disk (not in conversation messages), it survives context compaction. The Active Plan system prompt (injected into `_messages[0]` by `_rebuild_for_normal_mode`) instructs the agent to read the plan file before implementing and to re-read it if something feels unclear. This is the canonical mechanism for ensuring long-lived skill instructions survive multi-phase conversations.
- **Running indicator lifecycle**: `tui/app.py` has a 1-line `Static` widget (`#running-indicator`, `display: none` by default) between `#main-row` and `#prompt`. It is shown by `_show_running_indicator()` and hidden by `_hide_running_indicator()` (called from `_enable_input()`). The braille spinner (`_SPINNER` class var, 10 frames) is advanced by a `set_interval(0.1, _tick_spinner)` timer that no-ops when the bar is hidden. Show/hide call sites: `_dispatch_agent` (new task), `on_prompt_input_submitted` inject-and-resume branch (pause → resume), `on_prompt_input_submitted` ask_user answer branch (agent resumes after answer), `action_pause` (explicit hide without enabling input). Because `_enable_input` is always called via `call_from_thread`, `_hide_running_indicator` runs on the Textual main thread — safe.
- **`/clear` resets all session state**: The handler resets `_active_loop`, `_current_loop_ref`, `_stats`, sidebar stats, and sidebar plan subtasks. It guards against being called while the agent is running (`_worker.is_alive()`) — the user must ESC to pause first. After clear, the next dispatched task creates a fresh `AgentLoop` with `initial_messages=None`, a new `SessionTracker`, and only the system prompt in `_messages`.
- **`/hist` in TUI writes to hidden buffer**: `hist.run()` calls `console.print()` which writes to a Rich console that is not the Textual RichLog. Output appears in the terminal's hidden scroll buffer (behind Textual). This is a known limitation — `/hist` is functional but not displayed in the conversation pane.
- **Compaction summary is now a first-class JSONL record**: After the 2026-06-05 fix, each compaction fires `on_summary` → `tracker.record_user(summary_content)`, writing a `{"type": "message", "entity": "user", "content": "[CONTEXT SUMMARY ..."}` record into the JSONL stream. Previously the summary only appeared inside `session_end.raw_messages`. Parsing tools (`parse_jsonl_logs.py`, `parse_session_log.py`) will now encounter this record in the message stream — they already handle user messages correctly, so no changes needed there.

- **2026-06-26 Security Fix (2)**: Temp file leak on subagent timeout — `tools/_subagent_runner.py`.
  **Cause**: `run_subagent()` created a temp file (`mkstemp`) but only called `task_file.unlink()` when `_poll_until` did NOT return `"timeout"`. On timeout the subprocess was kept alive in `_active` for `resume_subagent()`, but the temp file path was a local variable that went out of scope — lost forever. `resume_subagent()` had no access to it. Long sessions with many subagent timeouts accumulated temp files indefinitely in the system temp dir.
  **Fix**: Added `task_file: Path` to `_SubagentState` so the path travels with the process handle. Moved `state.task_file.unlink(missing_ok=True)` into the `ret is not None` branch of `_poll_until` (the only place process exit is detected). Removed the conditional `unlink` in `run_subagent`. Cleanup now always fires exactly once regardless of which caller (`run_subagent` or `resume_subagent`) drives the final poll.

- **2026-06-26 Security Fix**: `tempfile.mktemp()` TOCTOU race at 3 sites.
  **Cause**: `mktemp()` reserves a filename without creating the file, leaving a window between the return and `write_text()` in which an attacker can create the file or a symlink — a CVE-class symlink attack. All three sites were in temp-file creation paths for subagent task and system-prompt content.
  **Fix**: Replaced all three sites with `fd, _tmp = tempfile.mkstemp(...)` + `os.close(fd)` + `Path(_tmp).write_text(...)`. Added `import os` to both `tools/_subagent_runner.py` and `tools/cli_subagent.py`. Files: `tools/_subagent_runner.py:96`, `tools/cli_subagent.py:70,73`.

- **2026-06-12 Error**: `ValueError: Dataset terminal-bench-core@head not found` (and `terminal-bench-core==head not found`) when running `harbor run`.
  **Cause**: `terminal-bench-core` was the old Terminal-bench 2 dataset name. Harbor's registry uses an `org/name` format (OCI package registry). The correct name is `terminal-bench/terminal-bench-2@latest`. Additionally, Harbor's CLI uses `@` as the version separator (not `==` which was terminal-bench syntax). Both bat files and `run_terminal_bench.bat` had stale dataset names.
  **Fix**: Updated all three bat files (`run_harbor.bat`, `run_terminus2.bat`, `run_terminal_bench.bat`) to use `terminal-bench/terminal-bench-2@latest`.

- **2026-06-12 Error**: `KeyError: "Model 'claude-sonnet-openrouter' not found in config.yaml."` when `harbor run` invoked `DagiAgent` without `DAGI_BENCH_MODEL` set.
  **Cause**: `benchmarks/harbor/agent.py` hardcoded `os.environ.get("DAGI_BENCH_MODEL", "claude-sonnet-openrouter")` as the fallback model key. `config_benchmark.yaml` only defines `local-llamacpp`. When run directly (not via bat file), no env var was set, so it tried the hardcoded fallback which wasn't in the config.
  **Fix**: Changed to `model_key = os.environ.get("DAGI_BENCH_MODEL") or None`. Passing `None` to `resolve_model_config` causes it to read `default_model` from `config_benchmark.yaml` — the config is now the single source of truth for the default.

- **2026-06-12 Bug**: `/clear` slash command left stale session state visible in the sidebar after clearing.
  **Cause**: The handler set `_active_loop = None` and cleared the visual pane, but did not reset `_current_loop_ref`. The 2 s `_poll_plan` timer kept firing against the old loop's `plan_file`, so plan subtasks from the cleared session continued to appear in the sidebar header. There was also no guard against calling `/clear` while the agent was actively running.
  **Fix**: Added `self._current_loop_ref = []` reset, `sidebar.update_plan([], "")` call, and an early-return guard that rejects `/clear` when `self._worker.is_alive()` (directing the user to ESC first). The confirmation message was also updated to "Context cleared — new session" for clarity.

- **2026-06-11 Feature**: Error-pause on exhausted transient API retries (TUI).
  **Cause**: When `api_error_retries` were exhausted in the TUI, the loop raised an exception. `_agent_work()` caught it but `self._active_loop = loop` was never reached (it sat after `loop.run()`), so the loop instance was discarded. The next task created a fresh `AgentLoop` from whatever `_active_loop._messages` existed before the failed run — losing all partial context from the failed task.
  **Fix**: Added `on_pause: Callable[[], None]` and `supports_pause: bool = False` to `AgentCallbacks`. The TUI sets `supports_pause=True` and wires `on_pause` to show a yellow pause banner and re-enable input. In `AgentLoop.run()`, when transient errors exhaust retries and `supports_pause` is True, the loop calls `on_pause()` + `self.pause()` + `continue` (back to the outer while) instead of raising — the loop blocks at `_pause_event.wait()`. The worker thread stays alive. The existing `inject_and_resume` path in `on_prompt_input_submitted` already handles the paused state, so the TUI routes the user's next message directly to the paused loop. CLI and subagents keep `supports_pause=False` → existing raise behavior fully preserved. Additionally moved `self._active_loop = loop` to before `loop.run()` in `_agent_work` so context is preserved even for non-transient raises (401, 403).

- **2026-06-08 Feature**: Transient API error retry with exponential backoff.
  **Cause**: A single 429 or 5xx response would abort the entire task, making long-running tasks fragile against rate limits and transient server errors.
  **Fix**: Wrapped the API call in a try/except block catching `APIConnectionError`, `APITimeoutError`, and `APIStatusError` (for codes 429, 500, 502, 503). Retries use exponential backoff (`2^attempt` seconds, capped at 60s) up to `api_error_retries` (default 3, configurable in `config.yaml`). Non-transient errors propagate immediately. The retry counter (`_error_retries`) resets per loop iteration, independent of the ghost-response retry counter (`_null_retries`). User-facing retry notifications are sent via `on_assistant_text`.

- **`README.md` now has a User Guide section** (added 2026-06-09) covering: starting a new project with `/init`, writing effective tasks, plan mode walkthrough, the full slash command reference, TUI pause/resume, using skills, context management, and best-practice tips. It sits between the Usage and Configuration sections.

- **`bash_backend` config field is now a documented no-op in `create_tool_registry`**: As of 2026-06-13, `BashTool` is always registered unconditionally. An injected `bash_tool` is registered *additionally* if provided — no conditional gate. The `bash_backend` field on `AgentConfig` has an explicit comment marking it as a no-op kept only for config file backwards compatibility. `config_benchmark.yaml` still sets `bash_backend: subprocess` — this is harmless. The injected tool is stored as `self._injected_bash_tool` on the loop and re-passed to `create_tool_registry` on normal-mode rebuilds.
- **Harbor async/sync bridge**: Harbor's `BaseEnvironment.exec()` is async; `AgentLoop.run()` is synchronous. `DagiAgent.run()` (in `benchmarks/harbor/agent.py`) runs `AgentLoop` in `run_in_executor` (thread pool), then uses `asyncio.run_coroutine_threadsafe(environment.exec(...), event_loop)` to bridge back from the thread to the event loop. The thread calls `.result(timeout=...)` on the returned `concurrent.futures.Future` to block until execution completes. This is safe because `run_in_executor` guarantees the thread is separate from the event loop thread — no deadlock.
- **Terminal-bench 2's `send_keys(block=True)` appends `; tmux wait -S done`**: When `block=True` and the last key is "Enter", `TmuxSession` transparently rewrites the command as `<cmd>; tmux wait -S done` and then calls `tmux wait done` to block until completion. `TmuxBashTool.run()` uses this exclusively — no polling needed. Timeout raises `TimeoutError`, which `TmuxBashTool` catches and returns as a formatted string.
- **`resolve_model_config` now accepts `config_path` and `project_path`**: `config_path: Path | None = None` lets callers override the default `config.yaml` (e.g. `config_benchmark.yaml` for benchmark runs). `project_path: Path | None = None` additionally loads `{project_path}/.dagi/config.yaml` and merges it over the root — project scalars win, model catalog entries are shallow-merged (project entries add or fully replace root entries). Both parameters are optional; all existing callers pass neither and are unaffected.
- **`load_raw_config` is now path-aware**: accepts `config_path: Path | None = None`, falling back to module-level `_CONFIG_PATH = Path("config.yaml")`. The `list_model_ids()` and `load_cli_config()` helpers still call it without arguments — they always read `config.yaml`.
- **Terminal-bench 2 token fields differ from DAGI's**: `AgentResult.total_input_tokens` / `total_output_tokens` (Terminal-bench) vs. `input_tokens` / `output_tokens` (DAGI's `MessageNode`). `DagiAgent` sums `MessageNode.input_tokens` for `entity == "assistant"` from `loop.tracker._messages` to build `AgentResult`.
- **`DagiAgent.name()` is a `@staticmethod @abstractmethod`** — Terminal-bench uses the returned string (`"dagi"`) to tag benchmark results on the leaderboard. It must be unique.
- **Harbor dataset naming: two distinct registries**: `org/name@ref` (slash-separated, e.g. `terminal-bench/terminal-bench-2@latest`) routes through the OCI **package registry** (public, no auth required). Plain `name@version` (no slash) routes through the Supabase **dataset registry** (requires `harbor auth login`). The Harbor CLI auto-detects the format by checking for `/` in the name. `terminal-bench-core` was the old Terminal-bench 2 name; the Harbor dataset is `terminal-bench/terminal-bench-2`.
- **Dual filesystem in Harbor benchmarks**: DAGI's file tools (`read`, `write`, `edit`, `find`, `grep`) operate on the **Windows host** filesystem only. Only the `bash` tool (via `HarborBashTool`) routes commands to the **Docker container**. Container paths like `/app/doomgeneric_mips` are inaccessible to file tools — the agent must use `bash("ls /app")`, `bash("cat /app/file.txt")` etc. for container file access. A capable model (Claude Sonnet) will do this naturally; weaker models (Gemma 4) tried the Windows file tools first and got stuck. Fix B (2026-06-13) added a `system_prompt_preamble` injected first in the system prompt (before soul/agents.md) that explicitly instructs the agent: use `harbor_bash` for all container file I/O, start with `harbor_bash("ls /app")`, never call `enter_plan_mode`. The preamble is set in `config_benchmark.yaml` and parsed into `AgentConfig.system_prompt_preamble`.
- **`DagiAgent` model key now falls back to `config_benchmark.yaml`'s `default_model`**: `model_key = os.environ.get("DAGI_BENCH_MODEL") or None` passes `None` to `resolve_model_config`, which then reads `default_model` from the benchmark config. Previously hardcoded to `"claude-sonnet-openrouter"`, which caused a `KeyError` when that key wasn't in `config_benchmark.yaml`.
- **There is a stale test directory** `C:UsersalexrDriverless_AGItests` (bad path) at the repo root — likely a Windows path mangling artifact, harmless but odd.
- **`config_benchmark.yaml` lives in `benchmarks/`**: Moved from project root on 2026-06-14. `benchmarks/harbor/agent.py` resolves it via `Path(__file__).parent.parent / "config_benchmark.yaml"`. Do not recreate it at the project root — the agent will silently use the wrong config.
- **`soul.md` now lives at `.dagi/prompts/soul.md`**: The Dagi-chan persona file was moved from repo root `soul.md` into `.dagi/prompts/soul.md` (commits `579aeca` → `ffdc752`). `load_soul(dagi_root, project_path)` now resolves it from there. Per-project persona overrides go in `{project_path}/.dagi/prompts/soul.md`. **Note:** `tui/utils.py:_system_breakdown()` still references the old `dagi_root / "soul.md"` path (line 66) — the sidebar's "sys-prompt" token count understates by ~150-300 tokens.
- **`agent/prompts.py` has project-aware helpers**: `load_main_system_prompt(dagi_root, project_path)` returns the project-local `main_system.md` if it exists, else the dagi root fallback. `load_soul(dagi_root, project_path)` follows the same pattern (project → dagi root → `None`). Both are pure Path lookups — no config file parsing. `dagi_root` is always `Path(__file__).parent.parent` from the caller's perspective.
- **Preamble assembly centralised in `_build_preamble(dagi_root)`** (fixed 2026-06-17): Extracted the full preamble stack (system_prompt_preamble + soul.md + dagi agents.md + project agents.md) into `AgentLoop._build_preamble(dagi_root: Path) -> str`. As of 2026-06-27, `_build_preamble` is called only from `_assemble_system_string` — the single canonical assembly entry point for the entire system prompt. Adding a 4th build site means calling `_assemble_system_string`, not `_build_preamble` directly.
- **Compaction failure crashes the session** — **FIXED 2026-06-21**: `CompactTool.compact()` (line 245 of `tools/compact.py`) re-raises exceptions after rolling back the message list. The rollback is correct (messages are preserved), but the re-raise propagated through `_compact_context()` (no handler) to the main loop's `except Exception`, killing the session. Fixed by wrapping `compact_tool.compact()` in `_compact_context()` with try/except — failure emits a warning via `on_assistant_text` and returns `_NO_COMPACTION`; the session continues with un-compacted messages.
- **Compaction dumps base64 image data into summarization prompt**: `_format_messages_for_summary()` in `compact.py` calls `str()` on list-typed tool message content (from `ReadTool` image results), producing Python repr of `[{"type": "image_url", ...}]` with raw base64 data. A single image can inject ~32K tokens of noise into the compaction summarization call. Fix: filter or truncate non-string content before formatting.
- **`thinking_tokens` (reasoning tokens) not recorded in session JSONL**: `SessionTracker.record_assistant()` captures `prompt_tokens`, `completion_tokens`, and `cost` but not `completion_tokens_details.reasoning_tokens`. The TUI shows thinking tokens via `on_token_update`, but the permanent JSONL record — the basis for all post-session analysis — doesn't capture this data. For extended-thinking models (DeepSeek, Claude), reasoning tokens can be 50%+ of the completion budget.
- **`_parse_frontmatter` is duplicated verbatim** between `agent/skills.py:30-42` and `agent/workflows.py:30-42` — same regex patterns, same function body, same docstring. Should be extracted to `agent/_frontmatter.py`.
- **`DAGI_ROOT` centralized in `agent/__init__.py`** — **FULLY FIXED 2026-06-27**: All independent `Path(__file__).parent.parent` computations have been replaced with `from agent import DAGI_ROOT`. Final 5 sites fixed: `cli.py:43` (module-level `_DAGI_ROOT`), `cli.py:804` (`_load_skill_map` local var), `tui/app.py:52` (`compose` local var), `tui/commands.py:20` (`_load_maps` local var), `tools/spawn_subagent.py:23` (module-level `_DAGI_ROOT`). `agent/__init__.py:3` is the sole canonical definition.
- **Tool result content not truncated in JSONL logs**: `record_tool_end(name, result_str)` writes the full result string. For large file reads or base64-encoded images, this can be enormous. Compare with `record_subagent_end` which truncates to 500 chars. The 198 accumulated session logs are the primary driver of disk consumption.
- **`__list__:` tool result encoding is undocumented**: `agent/loop.py:555` encodes non-string tool results as `"__list__:" + json.dumps(result)`. This convention is stored in JSONL logs and passed to callbacks. Downstream consumers must know this prefix to correctly handle image/multimodal tool results.
- **`SpawnCliSubagentTool` pipe buffer deadlock** — discovered 2026-06-25, **FIXED 2026-06-26**. `tools/cli_subagent.py` previously used a hand-rolled `Popen`+`proc.wait()` with no drain thread, no TUI relay, no PID tracking, and no timeout resume. Fixed by migrating fully to `run_subagent()`: prompt file passed via new `extra_argv` param, `on_event_factory`/`tracker` wired through constructor, prompt-file cleanup in `try/finally`, timeout now returns resumable PID dict. `SpawnCliSubagentTool` now has full feature parity with `SpawnSubagentTool`.
- **Subagent discovery only scanned project path, not dagi root** — discovered 2026-06-25, **FIXED 2026-06-27**. `_discover_subagent_tools()` now scans `DAGI_ROOT/.dagi/subagents` first (built-in types), then `cwd/.dagi/subagents` (project types, override on name collision). `cwd == DAGI_ROOT` is detected via path identity to skip the duplicate scan. Tools are collected into `dict[str, BaseTool]` keyed by type name so project entries naturally override built-in entries with the same name. When `project_path != DAGI_ROOT`, all four built-in subagent types (worker, review, explore_files, web_research) are now always available.
- **`_estimate_tokens` base64 `str()` inflates token counts** — discovered 2026-06-25. `tools/compact.py:45-52` calls `str(msg["content"])` on list-typed content (OpenAI vision format), producing a Python repr of `[{"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}]`. This massively inflates the token estimate, causing compaction to trigger earlier than necessary.
- **`_subagent_runner.py` pipe deadlock in CLI mode** — discovered 2026-06-25, **FIXED 2026-06-26**. The drain thread was only started when `on_event is not None`. In CLI mode `on_event` is `None`, so no thread read stdout — pipe filled after >64KB, child blocked, `_poll_until` polled `None` indefinitely until timeout. Fixed by adding `_drain_stdout()` and always starting a thread: `_stream_stdout` when a relay callback is present, `_drain_stdout` (read-and-discard) otherwise. Also added `extra_argv: list[str] | None` to `run_subagent()` to support additional CLI flags from callers.
- **`SkillTool.run()` reloads all skills from disk on every invocation**: `tools/skill.py:43-46` creates a new `SkillLoader`, scans all skill root directories recursively, reads every SKILL.md file, parses frontmatter, and builds the full skills map on every `skill("name")` call. `AgentLoop.skills` already has the pre-loaded list but `SkillTool` has no reference to it. Fix: pass the pre-loaded skills list at construction time or cache after first load. `reload_skills` already rebuilds the registry (creating a new `SkillTool`), so caching wouldn't prevent hot-reload.
- **`session_end` JSONL record dumps full `raw_messages` including base64 images**: `AgentLoop.finish()` passes `self._messages` to `tracker.finish(raw_messages=...)`, which serialises the entire conversation history (including all tool results with base64-encoded images) into a single JSONL line. A session with 5 images produces a 160KB+ `session_end` record. This is separate from the `record_tool_end` truncation issue (C6) — the `session_end` dump is a second copy of the same data, magnified because it includes ALL messages.
- **`WebFetchTool` silently upgrades HTTP to HTTPS for non-localhost private IPs**: `tools/web_fetch.py:123` excludes only `localhost` and `127.0.0.1`. Private network addresses (`192.168.*`, `10.*`, `0.0.0.0`, `[::1]`) are silently upgraded to HTTPS, causing connection failures on local development servers.
- **`json.loads(tc.function.arguments)` parsed 4 times per tool call** — **FIXED 2026-06-27**. Now parsed once: `args = json.loads(tc.function.arguments)` at line 547, reused at all 4 dispatch branches (registry dispatch, `_handle_enter_plan_mode`, `_handle_exit_plan_mode`, `_handle_switch_model`). Previously, malformed JSON would be caught at the first dispatch site but the sentinel branches re-parsed unconditionally — raising an uncaught `JSONDecodeError` rather than a clean tool error.
- **`_rebuild_for_reload` silently resets autonomous plan mode** — discovered 2026-06-26, **FIXED 2026-06-27**. `_rebuild_for_reload()` (line 909-912 of `agent/loop.py`) called `_rebuild_for_plan_mode(dagi_root, Path(self.config.plan_file))` without passing `interactive=`. The default `interactive=True` reset `plan_mode_initiated_by` from `"dagi"` to `"user"`, flipping `AskUserTool` timeout from 60s (auto-approve) to infinite — the autonomous agent hung at the next `ask_user` call. Fix: derive `interactive = self.config.plan_mode_initiated_by == "user"` before calling and pass it through.
- **`build_subagent_registry` crashes for non-DAGI projects** — discovered 2026-06-26, **FIXED 2026-06-27**. `_load_subagent_config` only checked `project_path / ".dagi" / "subagents"`. When spawning a worker/review subagent for a non-DAGI project, the child subprocess crashed with `ValueError`. Fix: `_load_subagent_config` now tries `project_path` first, then `_DAGI_ROOT` as fallback — uses a `for root in [project_path, _DAGI_ROOT]` loop with `config_path.exists()` guard. This is the subprocess-side fix for the same root cause as the parent-side `_discover_subagent_tools` issue (2026-06-25 A3).
- **`ToolRegistry.dispatch()` swallows system-level exceptions** — discovered 2026-06-26. `agent/registry.py:29-35` has `except Exception as e: return f"Error: {e}"` which catches `MemoryError` and `RecursionError` (both inherit from `Exception`). A tool that OOMs silently produces an error string instead of propagating the failure. Fix: re-raise `(MemoryError, RecursionError)` before the general `except Exception`.
- **BM25 `_bm25_scores` has quadratic df computation** — discovered 2026-06-26. `agent/memory_retriever.py:39-41` recomputes document frequency `n_q = sum(1 for t in tokenized if term in t)` inside a nested loop over documents × query terms, making scoring O(N²×M). Should precompute df per term before the scoring loop. Negligible for current wiki sizes (<50 pages), relevant at 500+.
- **`_effective_memory_root` inline recomputation in rebuild methods** — **FIXED 2026-06-27**: Both `_rebuild_for_normal_mode` and `_rebuild_for_plan_mode` recomputed the memory root locally instead of using `self._effective_memory_root`. Eliminated by the `_assemble_system_string` unification — the method reads `self._effective_memory_root` directly.
- **`_rebuild_for_plan_mode` missing `bash_tool=` is a NON-ISSUE** — discovered 2026-06-22, **corrected 2026-06-25**. The plan_mode branch of `create_tool_registry` (lines 232-272 of `agent/tools.py`) intentionally omits all bash tools and has no code path that uses the `bash_tool` parameter. Passing `bash_tool=` would have no effect.
- **3-site system-prompt divergence** — **FIXED 2026-06-27**: `__init__`, `_rebuild_for_normal_mode`, and `_rebuild_for_plan_mode` each independently assembled the system prompt across 8 steps, producing 4 confirmed bugs (soul/agents.md dropped, `memory_root` missing, `provider_order` leak, `_effective_memory_root` recomputed). Fixed by extracting `_assemble_system_string(dagi_root) -> str` — all 8 steps in one place; call sites handle only `_messages` assignment and `compact_tool.bind()`. Adding any new prompt parameter now requires touching exactly one method.
- **Plan skeleton code omits `## Execution Protocol` heading** — **FIXED 2026-06-27**: `_handle_enter_plan_mode` (line 616-631) now writes `"## Execution Protocol\n\n"` after `"## Verification\n\n"` in the scaffold. Previously the agent had to write this section from skill-instruction memory; if those instructions were compacted before plan creation, the heading was absent — undermining the 2026-06-21 compaction-immunity fix. The plan document is now self-contained regardless of compaction timing.
- **`AgentConfig.system_prompt` is now lazy-loaded**: The field defaults to `""` (empty string). `AgentLoop.__init__` checks `if config.system_prompt` — a non-empty value is used as-is (enables test injection and subagent overrides); an empty value triggers `load_main_system_prompt(dagi_root, config.project_path)` to load from disk. This removes the module-level `DEFAULT_SYSTEM_PROMPT` constant that was previously eagerly loaded at import time, and ensures project-local prompts are respected.
- **`SessionTracker` logs are project-scoped**: `logs_dir=config.project_path / ".dagi" / "logs"` is now passed at construction (root `SessionTracker` only — child trackers inherit from parent). Session JSONL files are written into the active project's `.dagi/logs/` rather than a global location.
- **Plan-mode web tools**: `web_search` and `web_fetch` are now registered unconditionally in plan mode (lines 251–255 of `agent/tools.py`), before the `if config is not None:` subagent block. The two direct tools and the subagent launchers (`web_research`, `explore_files`) are complementary — direct tools for quick single-call lookups, subagents for deep multi-step research. Previously direct tools only appeared in the no-config fallback path (tests), creating a silent test/production divergence.

- **`review-dagi-daily` scheduled task now edits TODO.md and PROJECT_CONTEXT.md directly**: As of 2026-06-26, the daily review agent no longer writes dated `_todo/todo_{date}.md` silo files. It runs a 4-phase protocol: (1) Orient — read git log, diff, PROJECT_CONTEXT.md, and TODO.md as deduplication index; (2) Explore freely — codebase signals + web research for security advisories and dependency updates; (3) Update TODO.md — add new findings, escalate items open >14 days, archive completed items >30 days old; (4) Compress PROJECT_CONTEXT.md — keep 5 most recent session entries verbatim, collapse older ones. The existing `_todo/` dated files are historical record only.
- **`config.example.yaml` `tools:` section documents all 22 tool names** (as of 2026-06-13): file tools (`read`, `grep`, `find`, `write`, `edit`, `copy`), shell (`bash`, `tmux_bash`), git (`git_status`, `git_commit`, `git_rollback`), planning (`enter_plan_mode`, `complete_plan`), agent/subagent (`ask_user`, `spawn_cli_subagent`, `extend_subagent_timeout`), model (`switch_model`), skills (`skill`, `run_skill_script`, `reload_skills`), web (`web_search`, `web_fetch`), UI (`emote`). All entries are commented out — omitting `tools:` enables everything. Note: `switch_model` requires `advanced_model`/`worker_model` to be set; `tmux_bash` requires an injected `bash_tool` at runtime — listing them is safe but they silently no-op if their conditions aren't met. `emote` is now always registered unconditionally; exclude it from `tools:` to suppress it.
- **RAM watchdog is CPython-specific**: `tests/conftest.py` uses `ctypes.pythonapi.PyThreadState_SetAsyncExc` to inject an exception into the test thread. This is a CPython C-API function — it won't work on PyPy or other interpreters. The 90% hard-kill via `os._exit(1)` is the universal fallback. The watchdog polls every 0.5 s with a daemon thread; it does not add measurable overhead (107 tests: 2.16 s with watchdog vs. ~2.1 s without).
- **MagicMock + `yaml.safe_load` = infinite loop**: Never pass a MagicMock (or any object with a truthy, zero-length `.read()` return) to `yaml.safe_load()`. The YAML reader's `determine_encoding` loop checks `len(raw_buffer) < 2` and `not eof`; MagicMock satisfies both forever. When mocking `AgentConfig` in tests, always set `project_path` to a real `Path` — it flows into `SpawnSubagentTool._load_parameters()` which calls `yaml.safe_load(config_path.read_text(...))`.
- **`requirements.txt` now documents hard vs optional deps**: Core (openai, pyyaml, python-dotenv, rich, typer, textual) are listed as required; web tools (ddgs, crawl4ai, httpx, beautifulsoup4) are commented-out optional entries. `pyproject.toml` still declares several deps (`nicegui`, `markdown`, `matplotlib`) not in requirements.txt or the conda env.

- **Scheduler interval unit is seconds (float-only)**: `parse_interval()` in `scheduler/models.py` accepts only a `float | int` in seconds. No string aliases — write `86400.0` for daily, `3600.0` for hourly, `1800.0` for 30 min, `60.0` for 1 min. Minimum is 60 s (1 minute). `ScheduledTask.interval: float`; `interval_td()` returns `timedelta(seconds=self.interval)`. YAML integers are coerced via `float(entry["interval"])`. Seconds was chosen over hours for exact arithmetic — `timedelta(seconds=60)` is lossless; `timedelta(hours=1/60)` accumulates floating-point error.
- **Scheduler safety: autonomous tasks cannot modify the schedule**: `schedule_task`, `list_scheduled_tasks`, and `remove_scheduled_task` are registered in `create_tool_registry` only when `plan_mode_initiated_by == "user"`. When the runner sets `plan_mode_initiated_by = "dagi"`, these tools are invisible to the agent — not just gated, but absent from the registry entirely. This is the same mechanism used to gate interactive vs autonomous plan mode.
- **Scheduler timeout uses abandoned daemon thread**: `scheduler/runner.py` runs each task on a `daemon=True` thread and joins it with `timeout=task.timeout_minutes * 60`. If the join times out, the thread is abandoned (Python has no `Thread.kill()`). Because the process exits when all tasks complete, the OS reclaims the thread. This is the standard Python pattern for best-effort timeout without `multiprocessing`.
- **`ask_user_timeout` on `AgentConfig` makes the scheduler's autonomy configurable**: Added at `agent/loop.py` (after `provider_order`). When set, `create_tool_registry`'s normal-mode branch uses this value for `AskUserTool(timeout=...)` instead of the hardcoded 300s. The runner sets it to 60s; interactive sessions get the default (300s via `None`). Adding new timeout contexts follows the same pattern: add a field to `AgentConfig`, read it in `create_tool_registry`.
- **Telegram bot package is `tg/`, not `telegram/`**: The `python-telegram-bot` library installs as `telegram` — naming the DAGI package `telegram/` would shadow it (circular import at `from telegram import Update`). The `tg/` name avoids the collision. All internal imports use `from tg import TelegramBot`.
- **Telegram bot async/sync bridge uses two proven patterns**: (1) `run_in_executor(None, loop.run, task)` dispatches the sync `AgentLoop` from the async Telegram handler (same pattern as `benchmarks/harbor/agent.py:75`). (2) `run_coroutine_threadsafe(bot.send_message(...), event_loop).result(timeout=30)` sends Telegram messages from the sync agent callback thread (same pattern as Harbor's `exec_command`). The event loop reference is captured in `_post_init` via `asyncio.get_running_loop()`.
- **Telegram `ask_user` uses the same `(Event, container)` pattern as TUI**: `tg/callbacks.py` creates `(threading.Event(), [])`, stores it on `session.pending_ask`, blocks on `Event.wait()`. The next Telegram message checks `pending_ask`, appends the answer, and calls `Event.set()`. Numeric answers are resolved to option labels (e.g., "2" → second option's label).
- **Telegram per-chat concurrency**: Each `ChatSession` has a `busy: bool` flag. A second message while the agent is running gets a "please wait" reply. Different chat IDs can run simultaneously on separate executor threads (the `SessionManager` dict is only mutated from the single-threaded asyncio event loop).
- **Telegram bot token follows `api_key_env` pattern**: `load_telegram_config()` reads `telegram.bot_token_env` from `config.yaml` (default: `TELEGRAM_BOT_TOKEN`) and resolves the actual token from the environment. The token is never stored in yaml.
- **Execution log is append-only JSONL at `.dagi/scheduler/runs.jsonl`**: Each record: `{"task": str, "started_at": ISO8601, "finished_at": ISO8601, "status": "success"|"timeout"|"error", "summary": str(<=500 chars)}`. `RunTracker` loads all records at init and builds a `dict[str, datetime]` of last-run times in memory. `is_due()` is then a single `datetime.now() - last >= task.interval()` comparison — no re-reads per check.

## Terms & Language

- **TASK_END / `<<TASK_END>>`**: Legacy sentinel string for task completion; kept as alias for `<<END_OF_RESPONSE>>`.
- **END_OF_RESPONSE / `<<END_OF_RESPONSE>>`**: Primary sentinel the agent appends to every no-tool-call response to signal it has finished its turn. The harness strips it before displaying output and exits the run loop cleanly. Formerly `<<AWAIT_USER_RESPONSE>>`.
- **continuation**: The harness injecting a `"continue"` user message when the agent stops without a termination flag — recovery mechanism for mid-task stalls.
- **compaction**: Pi-style summarization of the middle of `_messages` when context exceeds the token budget, preserving system prompt and recent tail.
- **tier**: One of `default`, `worker`, `plan` — the three model slots in `config.yaml` (`default_model`, `worker_model`, `advanced_model`). The loop switches tiers via `switch_model` sentinel.
- **TUI**: The new Textual-based terminal UI (`tui.py`). "Full TUI" in this project means a fixed-canvas multi-pane layout (unlike `cli.py` which is a scrolling REPL). Requires `textual>=0.80.0`.
- **GNHF**: "Good and not horrible feedback" — dagi's self-improvement workflow. Committed milestones, iterative development, freeform notes log at `.dagi/gnhf/notes.md`.
- **BM25**: Sparse keyword ranking algorithm that was previously used for memory retrieval. Removed 2026-06-27 — replaced by grep + wiki traversal via `memory-query` subagent.
- **memory-query subagent**: Replacement for the `memory-query` skill. Spawned via `spawn_memory_query_subagent`; restricted to memory_root only; uses Grep + Read to traverse the wiki index and return a synthesised answer with citations in a handoff file.
- **memory-add subagent**: Replacement for the `memory-add` skill. Spawned via `spawn_memory_add_subagent`; classifies content as project-specific (`wiki/projects/{name}/`) or general (`wiki/knowledge/{topic}/`); writes 5-field frontmatter nodes and updates `.index.md` hierarchy.
- **`root: memory_root` sentinel**: Field in `subagent_config.yaml` that tells `build_subagent_registry()` to restrict all file tools' `cwd` and `allowed_roots` to the resolved `memory_root` path. Prevents memory subagents from reading or writing outside the wiki.
- **wiki index auto-injection**: `_build_wiki_index_context(memory_root)` in `agent/loop.py` reads the root `wiki/.index.md` plus `projects/.index.md` and `knowledge/.index.md` and injects them as a system message before the first user turn in every session.
- **scheduler**: The `scheduler/` package — `models.py`, `tracker.py`, `runner.py`. Triggered by `run_scheduler.bat` (or any external trigger). Tasks are defined in `.dagi/scheduler/schedule.yaml` (interval in **seconds**) and tracked in `.dagi/scheduler/runs.jsonl`. The runner sets `plan_mode_initiated_by="dagi"` and `ask_user_timeout=60` for fully autonomous execution.
- **ScheduledTask**: Dataclass in `scheduler/models.py`. Fields: `name`, `prompt`, `interval: float` (seconds, end-to-start gap), `project_path`, `model`, `timeout_minutes` (default 30), `output_file`, `enabled`. `interval_td()` returns `timedelta(seconds=self.interval)`.
- **RunTracker**: Class in `scheduler/tracker.py`. Loads `runs.jsonl` at init; provides `is_due(task)` and `record_run(...)`. In-memory cache avoids repeated disk reads per due-check.
- **run_scheduler.bat**: Windows batch trigger at repo root. Calls `conda run -n dagi python -m scheduler.runner`. Wire into Windows Task Scheduler for automated execution. Supports `--dry-run` flag.
- **user-todo.md**: Root-level wiki file (`wiki/user-todo.md`) for tracking the Admiral's personal intentions and plans. Entries are numbered `[TODO-NNN]` with fields: Date Added, Date Due, Status (`pending`|`in-progress`|`completed`|`dropped`), Task Description, Proposed Method, Related Nodes. Append-only — status is updated, entries are never deleted. Created by `/init`; populated by the `memory-add` subagent's Step 0.5 TODO detection flow.
- **add-todo**: Log operation type in `wiki/log.md` for TODO entries appended by the `memory-add` subagent. Format: `[YYYY-MM-DD] add-todo | {task title} | wiki/user-todo.md#TODO-{NNN}`.
- **5-field wiki frontmatter**: The canonical wiki node schema: `type` (note/entity/source-summary/reflection/insight/analysis/context/update), `topic`, `description`, `date_added`, `tags` (comma-separated keywords). Replaced the old 8-field schema with confidence and binary tag fields.
- **Telegram bot**: The `tg/` package + `telegram_bot.py` entry point. Fourth interface to `AgentLoop` after TUI, CLI, and one-shot. Uses `python-telegram-bot` (async) with a threading bridge to sync `AgentLoop`. Personal-use, single-user design. Bot token from `@BotFather`, stored in `.env` as `TELEGRAM_BOT_TOKEN`.
- **IPC**: Inter-process communication between parent agent and subagent subprocess. Now pipe-based (stdout JSON events) via `tools/_subagent_runner.py`. The old file-sentinel IPC (`agent/ipc.py`) has been deleted.
- **Terminal-bench 2**: Benchmark of 89 real-world terminal tasks (software engineering, sysadmin, ML, security, etc.) run in Docker containers. Agents interface via `BaseAgent.perform_task(instruction, TmuxSession, logging_dir)`. Top scores ~60–65% as of 2026-06.
- **TmuxSession**: Terminal-bench object wrapping a tmux session inside a Docker container. Key methods: `send_keys(keys, block, max_timeout_sec)` and `capture_pane(capture_entire)`. When `block=True` + last key is "Enter", blocks until the shell command finishes via `tmux wait`.
- **bash_backend**: `AgentConfig` field (`"subprocess"`). Previously gated whether `BashTool` or an injected `_bash_tool` ran (`"external"` replaced `BashTool`). As of 2026-06-13, `create_tool_registry` always registers `BashTool` and additionally registers any injected `bash_tool` — the field is a no-op for tool registration. Kept in `AgentConfig` and `config_benchmark.yaml` for backwards compatibility only. The injected tool must be provided to `AgentLoop(_bash_tool=...)` at runtime; `bash_backend` plays no role in that decision.
- **Harbor Framework**: The successor to Terminal-bench 2. Agents implement `harbor.agents.base.BaseAgent` with an `async run(instruction, environment, context)` interface. Shell execution goes through `await environment.exec(command)` rather than a `TmuxSession`. The official leaderboard is at harborframework.com. DAGI's Harbor adapter lives in `benchmarks/harbor/`.
- **Harbor package registry**: OCI-based public registry; datasets referenced as `org/name@ref` (with `/`). No authentication required. `terminal-bench/terminal-bench-2@latest` is the 89-task benchmark dataset.
- **Harbor dataset registry**: Supabase-backed; datasets referenced as `name@version` (no `/`). Requires `harbor auth login`. Harbor CLI auto-detects which registry to use by checking for `/` in the name.
- **emote**: One of five named expressions (`default`, `confused`, `happy`, `serious`, `funny`) dagi-chan can display in the sidebar. Stored as plain-text `.md` files in `.dagi/emotes/`; switched by the agent via the `emote` tool.

---

## Claude's Insights

> Independent observations — not highlighted by the user.

### User Tendencies

- Invests in structural cleanup (refactoring tui.py → tui/ package) proactively once a module exceeds ~800 lines, rather than waiting for it to become unmanageable. Refactors are purely organizational — behaviour is preserved exactly.
- **Review velocity outpacing fix velocity** (observed 2026-06-26): 7 daily reviews have accumulated 18 persisting items (including 2 CRITICAL security issues open **19 days**), zero commits in **5 days**. The review system generates findings faster than they are resolved — ~3 new items per review vs. 0 fixes applied. Uncommitted changes are 5+ days old, risking data loss. A focused fix sprint clearing the 10 XS-effort items (~4 hours) would halve the persist table. The self-improvement mechanisms (GNHF loop, self-review) have been dormant for **62 days** despite 198 unanalysed session logs — this is the largest gap between the project's stated objective ("self-improving agent") and its current trajectory.
- Ships incrementally and tests at each step; does not batch large refactors.
- Has a strong preference for maintaining backward compatibility — new features are additive, never breaking (`cli.py` kept alongside `tui.py`).
- Tends to work directly on `main` rather than feature branches.
- README and TODO are kept scrupulously up-to-date — the user treats them as living documents, not afterthoughts.
- Prefers explicit, non-magical configuration (env var pointers in yaml rather than magic env var names) but occasionally wants the escape hatch of inlining secrets directly.
- Engages deeply in design grilling before implementation — responds well to adversarial questioning about trade-offs and commits to concrete choices before coding begins. Does not want vague or open-ended design left to implementation time.
- Prefers pause semantics that preserve agent context (inject & resume) over simpler cancel-and-restart approaches, even at slightly higher implementation cost. Favours architectural correctness over convenience shortcuts.
- Will accept dead-code cleanup (removal) when shown the evidence; prefers the simpler behavior (accept the extra round-trip) over adding new flag logic when the outcome is equivalent.
- Prefers behavioral unification over performance micro-optimisation — accepted Option C (one extra LLM round-trip per slash command) because it eliminates divergent code paths, rather than synthetic-prefill options that would save the round-trip at the cost of message-history surgery.
- Invests heavily in skill design: treats skills as behavioral specifications rather than code.
- Actively prunes redundant config fields once a general mechanism covers them — removed `emote_tool: true/false` immediately once the `tools:` list made it redundant. Prefers one orthogonal mechanism over N specialized flags.
- Strongly prefers config-driven behavior over code-injection parameters. When designing the Terminal-bench integration, immediately pushed back on a `bash_tool` parameter injection approach in favor of a `bash_backend` field in config.yaml — the config is the single source of truth for which implementation runs.
- Actively considers Windows-compatibility when reviewing cross-platform designs; caught the potential clash between `TmuxBashTool` and normal Windows usage before implementation began. Will design multi-phase skills (gather → interrogate → record) with explicit internal vs. external phases and mode-routing logic. The grill-me skill upgrade (session 17) is the clearest example — a thin 13-line prompt was replaced with a structured protocol incorporating Socratic method, knowledge-gathering, and automatic recording.
- Naturally gravitates toward Dependency Inversion: favors abstracting over a concrete type (`BaseTool`) rather than threading a specific impl through the stack. Accepted the `_bash_tool: BaseTool | None` generalization immediately — reducing two separate adapters (`TmuxBashTool`, `HarborBashTool`) to a single injection point is worth the minor parameter rename.

### Project Shortcomings

- **Soul/agents.md dropped after plan-mode transitions** — **FIXED 2026-06-17**: `_rebuild_for_normal_mode` and `_rebuild_for_plan_mode` were skipping soul.md and both agents.md files when reassembling the system prompt. Fixed by extracting preamble assembly into `AgentLoop._build_preamble(dagi_root)`, called from all 3 build sites (`__init__`, `_rebuild_for_normal_mode`, `_rebuild_for_plan_mode`). The full preamble stack (system_prompt_preamble + soul.md + dagi agents.md + project agents.md) is now always present after plan-mode transitions.
- **`_rebuild_for_normal_mode` missing `memory_root=` in `create_tool_registry` call** — discovered 2026-06-18, **FIXED 2026-06-26**. Same class of divergence bug as the `_build_preamble` issue: `__init__` (line 260) and `_rebuild_for_plan_mode` (line 846) both pass `memory_root=self._effective_memory_root`, but `_rebuild_for_normal_mode` (line 779) omitted it. After a plan→normal transition with a custom `memory_root` configured, `SkillTool` silently reverted to the default `project_path / "dagi-memory"`. Fix: added `memory_root=self._effective_memory_root,` at line ~790. All three `create_tool_registry` call sites are now symmetric. See `_todo/todo_2026-06-18.md` A1.
- **Session cost tracking almost always blank** — `SessionTracker.record_assistant()` reads `usage.cost` which most providers (including OpenRouter for many models) don't populate. The sidebar shows `$—` and `session_end` records have `total_cost: null`. No cost visibility makes it impossible to benchmark token efficiency or compare model tiers. See `_todo/todo_2026-06-18.md` C1.
- **`/hist` slash command in TUI is broken** — it writes to a `rich.Console` behind Textual's canvas. Needs reimplementing to write to `ConversationPane`.
- **`ask_user` returns verbatim free text**: The TUI no longer calls `_resolve_option()` to coerce the user's answer to a matching option label. `container.append(raw)` passes the raw string directly, so the agent's tool result JSON always contains the unmodified answer. Options are shown as hints only. The user's answer is now echoed as a cyan "You" panel in the ConversationPane immediately before the agent thread is unblocked.
- **`_resolve_option` has been deleted**: `tui/utils.py` no longer exports this function. The CLI paths (`on_ask_user` sync and `_render_queue` threaded) already passed raw text; option resolution only survived as their timeout-fallback, which is unchanged.
- **`PromptInput` has no placeholder**: Textual's `TextArea` (which `PromptInput` subclasses) does not expose a `.placeholder` property. The "Your answer…" cue shown during `ask_user` prompts was silently dropped. A future improvement could overlay a `Label` or use the TUI's conversation pane to communicate prompt context.
- **Error-pause implemented (2026-06-11)** — TUI now pauses the session (same semantics as ESC pause) when transient retries are exhausted, rather than dying and losing context. Non-transient errors (401, 403) still raise; context is preserved via the `_active_loop` pre-assignment fix.
- **`max_continuations` was silently hardcoded at 10** until 2026-06-05 — it existed as an `AgentConfig` default but was never read from `config.yaml`. Now configurable via top-level `max_continuations:` key.
- **Dependency split between `requirements.txt` and `pyproject.toml`** — `requirements.txt` (pip freeze of actual `dagi` conda env) has only 23 packages; `pyproject.toml` declares ~10 more (`ddgs`, `crawl4ai`, `beautifulsoup4`, `nicegui`, `markdown`, `matplotlib`, `typer`, `rich`). Neither file alone produces a working install. The `dagi` conda env is missing several declared runtime deps, meaning tools like `web_search`, `web_fetch`, and the Rich CLI may silently fail until those packages are installed.
- **BashTool is unsandboxed** — no command blacklist, no process group kill on timeout. An agent could run destructive bash commands. Path guard protects file tools but not bash.
- **No pause during subagent execution**: ESC pauses the *parent* loop at its checkpoint, but the subagent subprocess continues unaffected. A future improvement could write an IPC message to the subagent's stdin or use `proc.terminate()` when the user pauses.
- **`harbor_bash` must be in `config_benchmark.yaml` tools list**: After commit `c40b9b1`, the Harbor bash tool has `name="harbor_bash"`. If `tools:` lists `bash` instead, `filter_to` silently removes the container tool and the agent uses host subprocess bash — wrong results with no error. Always verify the tools list matches the injected tool's `.name` after any rename.
- **`run_harbor.bat` requires `PYTHONUTF8=1`**: conda on Windows CP1252 will crash with `UnicodeEncodeError` if harbor produces any rich Unicode output (box-drawing chars in error/result tables). The flag is now set in the bat file.
- **Harbor dual-filesystem mismatch**: In Harbor benchmark runs, DAGI's file tools (`read`/`write`/`find`/`grep`/`edit`) operate on the Windows host while only the `harbor_bash` tool routes to the Docker container. The project root in the agent's system prompt points to a local Windows job directory — not the container's `/app`. Weaker models try Windows file tools on container paths and get stuck. Fix: instruct the model explicitly (system prompt addition in `config_benchmark.yaml`) to use bash for all file I/O when running in Harbor mode.
- **RAM watchdog is fragile on memory-constrained machines**: The conftest watchdog fires at 70% system RAM. On a 31 GB machine running Claude Code + IDE + browser, idle RAM already sits at 72–73%. The watchdog fires during test *thread startup* (not test body), injecting `_RAMExceeded` before `yield`. All tests in the suite fail simultaneously with no useful signal. The threshold is hardcoded — there is no env-var override or `--no-watchdog` flag. A configurable threshold (e.g. `RAM_WARN_PCT = int(os.getenv("DAGI_RAM_WARN_PCT", "70"))`) would eliminate this class of false failure.
- **Compaction failure crashes the session** — **FIXED 2026-06-21**. `_compact_context()` in `agent/loop.py` now catches all exceptions from `compact_tool.compact()`, emits a user-visible warning via `on_assistant_text`, and returns `_NO_COMPACTION` so the session continues. The session may hit a context-length error on the next API call if the context is truly full, but that is handled by the existing transient retry logic rather than dying silently. See `_todo/todo_2026-06-20.md` A1.
- **Base64 image data in compaction summarization prompt** — discovered 2026-06-20. `_format_messages_for_summary()` in `tools/compact.py` lines 56-74 calls `str(msg["content"])` when content is a list (OpenAI vision format with base64 image data). Each image dumps ~32K tokens of raw base64 into the summarization prompt, wasting context and money while providing no useful information. Images should be replaced with a placeholder like `[image: <filename>]`. See `_todo/todo_2026-06-20.md` A2.
- **`thinking_tokens` not recorded in JSONL session logs** — discovered 2026-06-20. `SessionTracker.record_assistant()` captures `prompt_tokens` and `completion_tokens` but not `thinking_tokens` (reasoning token count from models with extended thinking). Session cost analysis and model comparison are incomplete without this metric. See `_todo/todo_2026-06-20.md` C1.
- **No integration tests** — all tests are unit tests with mocked LLM clients. There is no end-to-end test that runs a real agent loop against a live or recorded API response.
- **`temp_system_prompt.txt`, `temp_test.ipynb`, `plan.md` at root** are stale scratch files that should be cleaned up or archived.
- **`show_plan` tool was underused** — it already implemented the full plan revision loop (show → ask → revise → repeat), but `main_system.md` never instructed the agent to call it before `exit_plan_mode`. The tool was wired correctly; the orchestration was missing from the prompt.

- **2026-06-06 Refactor**: Subagent architecture rewritten from IPC+terminal-window model to pipe-based subprocess model.
  **Cause**: `agent/ipc.py` used file-sentinel polling + `CREATE_NEW_CONSOLE` Windows-only terminal spawning — complex, platform-specific, and the multi-task-per-terminal capability was unused.
  **Fix**: Deleted `agent/ipc.py` and `tools/_terminal_subagent.py`. New `tools/_subagent_runner.py` spawns subprocesses with `stdout=PIPE`; daemon thread reads newline-delimited JSON events and relays them to the main TUI `ConversationPane`. Timeout polling uses `proc.poll()` every 2 s. New `tools/extend_timeout.py` (`ExtendSubagentTimeoutTool`) lets the agent extend an in-flight subagent's deadline. `SpawnSubagentTool` generates the handoff path (`.dagi/handoffs/{type}_{uuid8}.md`); path returned to parent as tool result after subagent exits. Each subagent type now declares its tools explicitly in `.dagi/subagents/<type>/subagent_config.yaml` (`tools:` list replaces `scope:` preset). `_scope_tools()` deleted; `_tools_from_list()` added.

- **2026-06-05 Fix**: Compaction summary was never written to the JSONL session log.
  **Cause**: `_compact_context()` called `compact_tool.compact()` and mutated `_messages` in-place, but never called `tracker.record_user()`. The summary only appeared in `session_end.raw_messages` (the final snapshot), not as a standalone record in the JSONL stream. Both the auto-compact trigger and the `/compact` slash command bypassed logging.
  **Fix**: Added `_on_summary: Callable[[str], None]` callback to `CompactTool` (field in `__init__`, parameter in `bind()`). Fired inside `compact()` immediately after `msgs[head_end:tail_start] = [summary_message]`. All 4 `compact_tool.bind()` call sites in `agent/loop.py` now pass `on_summary=self.tracker.record_user`, covering both auto and manual compaction paths.

- **2026-05-31 Bug**: Input pane invisible/disabled during `ask_user` tool invocation — user could see the question but could not type an answer.
  **Cause**: `_dispatch_agent()` disables the input pane (line 644 in `tui.py`) when the agent starts. `_show_ask_user()` displayed the question but never called `_enable_input()`, so the input remained disabled for the duration of the ask. The input was only re-enabled in the `finally` block when the agent finished entirely.
  **Fix**: Added `self._enable_input()` at the end of `_show_ask_user()`. Added `self.query_one("#prompt", PromptInput).disabled = True` in `on_prompt_input_submitted()` after the pending-ask branch resolves, so the input is disabled again while the agent continues running.

### Assumptions to Challenge

- **Single-user, single-session**: no locking on `config.yaml` or session logs; running two dagi instances simultaneously against the same project could corrupt state.
- **OpenAI-compatible API contract**: assumes the provider's `/chat/completions` response schema matches the OpenAI SDK's expectations exactly. Providers sometimes diverge (e.g., `reasoning_content` field is non-standard).
- **English-only tasks**: system prompt and skill docs are English-only; non-English tasks may produce degraded results depending on the underlying model.

### Dependencies & Risks

- **OpenRouter** is the primary API gateway for most catalog models. A rate limit, outage, or pricing change would affect all non-OpenAI models simultaneously.
- **`ddgs` (DuckDuckGo search)**: unofficial API wrapper, no SLA, can break on site changes. Already listed as `>=2.0` which suggests prior breakage.
- **`crawl4ai`**: heavy dependency (Playwright-based), version-pinned at `>=0.4`. Breaks are likely as the web changes.
- **Python 3.14** in the `dagi` conda env — this is a pre-release / bleeding-edge version. Some packages may not have wheels for 3.14 yet (observed: `pytest` was missing, `pyyaml`/`openai` had to be installed manually).

### Potential Areas of Exploration

- **Fix `/hist` in TUI**: reimplement `_cmd_hist()` in `tui.py` to write session history directly to `ConversationPane` rather than calling `hist.run()` (which uses a bare `rich.Console`).
- **`ask_user` UX in TUI**: the user's free-text answer is now echoed in the conversation pane. The remaining gap: since `PromptInput` has no placeholder support, there is no visual cue distinguishing a normal prompt from an `ask_user` question prompt while the cursor is idle. A small indicator (e.g., a highlighted label or border colour change on `#prompt`) would improve clarity.
- **Parallel subagent dispatch**: the `[~]` in-progress state was introduced with parallel multi-agent future in mind. The IPC layer (`agent/ipc.py`) would need to support multiple concurrent polls; the plan panel would then show multiple subtasks as `[~]` simultaneously.
- **Pause during subagent execution**: current pause only stops the *parent* loop at its checkpoint. If a subagent is running (terminal subprocess), it continues unaffected. A future improvement could write an IPC `pause` sentinel to the subagent's channel.
- **Cache hit visibility**: `cache_prompt: true` is now sent to OpenRouter for all models, but the token tracker (`agent/session.py`) does not yet read `usage.prompt_tokens_details.cached_tokens` from the response. Displaying cached vs. non-cached tokens in the TUI sidebar would make caching ROI visible.
- **Streaming responses**: the TUI sidebar's token counter currently only updates after each full API response. Streaming would enable per-token updates and reduce perceived latency. The `ConversationPane` could stream assistant text incrementally using `RichLog.write()` calls on each chunk.
- **Structured output / tool-call validation**: the agent currently relies on the model to produce valid JSON for tool arguments. A schema-level validator at the registry layer would catch malformed calls early.
- **Session replay / dry-run mode**: the JSONL session log has everything needed to replay a session deterministically — useful for debugging and regression testing.
- **Parallel subagent dispatch**: `SpawnSubagentTool` currently blocks until the subprocess exits. Multiple concurrent subagents could be dispatched by the agent calling `spawn_*` multiple times and then polling each `extend_subagent_timeout` — no architectural change needed, just agent-level orchestration and TUI labelling per subagent.
- **Emote animation**: `_status_col()` runs on every `refresh()`. Animating the face or label (e.g., cycling symbols on `_status == "running"`) requires only a branch on `self._status` inside `_status_col()` — no new state or callbacks needed. The ASCII hair glyphs (`≋`) were removed when the logo moved inline; add them back here if desired.
- **Emote tool in CLI mode**: `EmoteTool` is always registered (unconditionally since 2026-06-13). In CLI mode, `on_emote=None` is passed since there is no sidebar — the tool still returns `"*emote*"` in text and the callback is a silent no-op. To disable `emote` entirely, exclude it from the `tools:` list in `config.yaml`.
- **Harbor smoke test completed 2026-06-12**: `benchmarks/harbor/agent.py` ran end-to-end against `terminal-bench/terminal-bench-2@latest` (task: `make-mips-interpreter`). 1 trial, 0 exceptions, reward 0.0. The `0.0` is expected — local Gemma 4 is not strong enough for complex Harbor tasks and was confused by the dual-filesystem environment. The async/sync bridge (`run_in_executor` + `run_coroutine_threadsafe`) worked correctly. To get meaningful scores, run with Claude Sonnet via `set DAGI_BENCH_MODEL=claude-sonnet-openrouter`.
- **`benchmarks/` has minimal unit tests**: `HarborBashTool.name` and `TmuxBashTool` are now covered by `tests/test_bash_tools.py`, but both `DagiAgent` adapters (Terminal-bench and Harbor) still have zero test coverage. A mock unit test for `DagiAgent.perform_task()` would catch regressions without needing Docker.
- **No full benchmark run yet**: Smoke test was done with `--n-tasks 1`. A full 89-task run has not been performed. Scores, token costs, and per-task performance data are unknown.

- **2026-06-13 Change**: `pyproject.toml` lacked `[tool.pytest.ini_options]` — tests could not import project modules.
  **Cause**: No `pythonpath` or `testpaths` declared; `pytest` ran from a path where `agent`, `tools`, etc. were not on `sys.path`. Existing tests shared this issue.
  **Fix**: Added `[tool.pytest.ini_options]` with `pythonpath = ["."]` and `testpaths = ["tests"]` to `pyproject.toml`. All 99 tests now pass without manual `PYTHONPATH` manipulation.

- **2026-06-13 Change**: `bash` and injected bash tools now coexist in the registry.
  **Cause**: `create_tool_registry()` registered either `BashTool` (subprocess) *or* the injected `bash_tool` (external) — never both. This meant benchmark adapters replaced the host `bash` tool entirely instead of augmenting it.
  **Fix**: Removed the `bash_backend == "external"` conditional. `BashTool(cwd=cwd)` is always registered first. If `bash_tool is not None`, it is *additionally* registered alongside it. `HarborBashTool.name` renamed `"bash"` → `"harbor_bash"` to eliminate the name conflict that would have caused a registry collision under the new logic.

- **2026-06-13 Promotion**: `TmuxBashTool` promoted to `tools/tmux_bash.py` as first-class tool.
  **Cause**: It lived only in `benchmarks/terminal_bench/tmux_bash_tool.py` with `name = "bash"`, making it unusable alongside `BashTool` without a name conflict.
  **Fix**: Created `tools/tmux_bash.py` with `name = "tmux_bash"`. Updated `benchmarks/terminal_bench/agent.py` to import from the canonical location. Original file kept intact for backwards compatibility.

- **2026-06-13 Code Review Fix**: `benchmarks/terminal_bench/tmux_bash_tool.py` retained stale `name = "bash"` after the canonical copy was promoted to `tools/tmux_bash.py`.
  **Cause**: The legacy file was kept as a reference but its `name` was not updated, creating a collision hazard if accidentally imported.
  **Fix**: Renamed `name = "bash"` → `name = "tmux_bash"` and updated the description to reference "tmux" explicitly.

- **2026-06-13 Code Review Fix**: `docs/terminal-bench.md` described the old `bash_backend: tmux` / `_tmux_session=session` integration pattern that no longer exists.
  **Cause**: Docs were not updated when the injection pattern changed from `bash_backend` config-driven replacement to `_bash_tool=bash_tool` additive injection.
  **Fix**: Replaced `bash_backend: tmux` yaml block and explanation with the new injection-pattern description; updated architecture diagram to show `_bash_tool=bash_tool` and both `BashTool` + `TmuxBashTool` registered.

- **2026-06-13 Cleanup**: Removed `emote_tool: true/false` config flag.
  **Cause**: Redundant now that `tools:` list controls which tools are registered. A dedicated boolean for a single tool is inconsistent with the general-purpose filter.
  **Fix**: Deleted `emote_tool` field from `AgentConfig` and its parsing from `config_loader.py`. `EmoteTool` is now always registered unconditionally in `create_tool_registry`. Users who want to suppress it can exclude `emote` from the `tools:` list. Removed the field from `config.example.yaml`, `config_benchmark.yaml`, `docs/terminal-bench.md`, `scripts/diagnose_registry.py`, and `tests/test_tool_filter.py`.

- **2026-06-13 Bug**: `run_harbor.bat` crashed immediately with `UnicodeEncodeError: 'charmap' codec can't encode character '�'` when run from Windows CMD.
  **Cause**: `conda run` relays subprocess stdout through Python's `print()`, which uses the console's CP1252 encoding on Windows. Harbor's rich TUI output contains Unicode box-drawing characters (`┌─┐│`) that CP1252 cannot encode. `run_terminus2.bat` was unaffected because a successful terminus-2 run produces output that keeps Unicode chars out of the conda relay path.
  **Fix**: Added `set PYTHONUTF8=1` to `run_harbor.bat` before the `conda run` call. This forces UTF-8 throughout the entire conda→harbor pipeline.

- **2026-06-13 Bug**: `run_harbor.bat` ran agent commands on the Windows host instead of the Harbor Docker container, silently producing wrong results (reward 0.0 in all tasks even for trivial ones).
  **Cause**: Commit `c40b9b1` renamed `HarborBashTool.name` from `"bash"` to `"harbor_bash"` to eliminate a registry collision — but `config_benchmark.yaml`'s `tools:` allowlist was not updated. The `filter_to(config.tools)` call in `create_tool_registry` kept `BashTool` (name=`"bash"`) and silently removed `HarborBashTool` (name=`"harbor_bash"`). The agent dispatched all shell commands via the host subprocess `BashTool`.
  **Fix**: Replaced `- bash` with `- harbor_bash` in `config_benchmark.yaml`'s `tools:` list, and added a comment explaining that the host `bash` tool is intentionally excluded from Harbor runs.

- **2026-06-13 Observation**: RAM watchdog in `tests/conftest.py` fires during test *setup* (not the test body) when system RAM is already ≥ 70% at process start. The watchdog daemon starts, immediately sees RAM ≥ threshold, and injects `_RAMExceeded` into the test thread while it is blocked inside `threading.Thread.start()` → `_started.wait()`. Result: all 5 tests error with `conftest._RAMExceeded` before any test code runs. Workaround: pass `-p no:cacheprovider` (or close background apps to free RAM). **Not a bug in the tests** — the machine was at 72.7% on a 31 GB system (other processes holding ~22.6 GB). Tests passed once RAM dropped below 70%.

- **2026-06-13 Feature**: `system_prompt_preamble` now injected into system prompt at all 3 build sites.
  **Cause**: Tasks 1 and 2 added `system_prompt_preamble: str = ""` to `AgentConfig` and wired it through the config loader, but the field was never actually injected into the assembled system prompt sent to the LLM. `__init__` used `preamble_parts` (the right mechanism); `_rebuild_for_normal_mode` and `_rebuild_for_plan_mode` built `new_system` directly without consulting the field.
  **Fix**: In `__init__`: prepend `config.system_prompt_preamble.strip()` to `preamble_parts` before soul text (so it arrives first). In both rebuild methods: prefix `new_system` with `preamble.strip() + "\n\n---\n\n"` when the field is set. 3 regression tests added to `tests/test_harbor_harness.py` (`TestPreambleInjection`); 124/124 pass.

- **2026-06-13 Feature**: Added `load_main_system_prompt` and `load_soul` to `agent/prompts.py`.
  **Cause**: Per-project config work requires that agents running inside a project folder can carry a custom system prompt and persona without editing the dagi root prompts.
  **Fix**: Two new helpers resolve `{project_path}/.dagi/prompts/main_system.md` (or `soul.md`) first; fall back to the dagi root equivalents. `load_soul` returns `None` when neither location exists. 5 tests added in `tests/test_project_config.py`; all pass.

- **2026-06-13 Code Review Fix**: `AgentConfig.bash_backend` comment falsely implied the field still drove tool registration.
  **Cause**: Comment not updated when the `bash_backend == "external"` conditional was removed from `create_tool_registry`.
  **Fix**: Replaced comment with explicit no-op documentation noting the field is kept only for config file backwards compatibility.

- **2026-06-13 Code Review Fix**: `_FakeBashTool` in `tests/test_bash_tools.py` did not subclass `BaseTool`.
  **Cause**: Test fake was written as a plain class with manually duplicated `schema()` method, bypassing ABC enforcement.
  **Fix**: Made `_FakeBashTool(BaseTool)` with proper `_parameters` class attribute and `run(**kwargs)` signature. Also removed unused `MagicMock` import.

- **2026-06-13 Bug**: `TestConfigDrivenFilter` tests in `tests/test_tool_filter.py` consumed 100% RAM and killed the machine.
  **Cause**: The test's `_config()` used `MagicMock()` without setting `project_path`. When `SpawnSubagentTool._load_parameters()` accessed `config.project_path`, MagicMock auto-generated it as another MagicMock. This mock flowed through `__truediv__` (path joining) and `read_text()`, producing a MagicMock that was passed to `yaml.safe_load()`. PyYAML's `Reader.determine_encoding()` entered an infinite loop: `stream.read(4096)` returned a truthy MagicMock (so `eof` never became True), and `len(MagicMock())` returned 0 (so the `while len(raw_buffer) < 2` condition was always True). Each iteration allocated new MagicMock objects → unbounded memory growth → OOM. The `except (FileNotFoundError, OSError, yaml.YAMLError)` handler never fired because the loop never raised.
  **Fix**: (1) Added `cfg.project_path = Path(".").resolve()` to the test mock. (2) Hardened `SpawnSubagentTool._load_parameters()` to guard `config.project_path` with `isinstance(proj, Path)` before using it — non-Path values are skipped, falling through to the `_DAGI_ROOT` search path.

- **2026-06-13 Code Review Fix**: `dagi_root` parameter was dead code in `load_main_system_prompt()` (agent/prompts.py).
  **Cause**: Function accepted `dagi_root` but its fallback path called `load_prompt("main/main_system.md")`, which used the module-level hardcoded `_PROMPTS_DIR` instead of the passed parameter.
  **Fix**: Replaced `return load_prompt("main/main_system.md")` with `return (dagi_root / ".dagi" / "prompts" / "main" / "main_system.md").read_text(encoding="utf-8")` to use `dagi_root` directly.

- **2026-06-13 Code Review Fix**: Brittle test assertion in `test_load_soul_falls_back_to_dagi_root()` coupled test to production file content.
  **Cause**: Test asserted `"Dagi-chan" in result`, binding the test logic to a specific persona file value that can change independently.
  **Fix**: Replaced content assertion with `len(result) > 0` to verify only that a file was loaded and is non-empty, not its specific content.

- **2026-06-13 Feature**: `resolve_model_config` now accepts `project_path: Path | None = None`.
  **Cause**: Per-project config work (Task 3) requires that a project folder's `.dagi/config.yaml` can override the root `config.yaml` for scalars (e.g., `max_continuations`) and extend the model catalog.
  **Fix**: Added `_load_project_config(project_path)` (returns `None` if absent, raises `ValueError` on invalid YAML) and `_merge_configs(root_raw, project_raw)` (shallow-merge model catalogs; project scalars win) to `agent/config_loader.py`. `resolve_model_config` calls both after loading the root config when `project_path` is not `None`, then sets `cfg.project_path = project_path` via `replace()`. All existing callers continue to work unchanged (default `project_path=None`). 4 new tests in `tests/test_project_config.py`; 9 total, all pass.

- **2026-06-13 Refactor**: All `resolve_model_config` call sites now pass `project_path=` directly.
  **Cause**: Task 3 added `project_path: Path | None = None` to `resolve_model_config`, but the 5 existing call sites still called the function without it and then manually assigned `config.project_path = project_path` on the returned dataclass. The manual assignment was redundant (and semantically wrong — it bypassed the `replace()` logic inside `resolve_model_config`).
  **Fix**: Updated `cli.py` (2 sites: `_run_task` and the subagent dispatch path), `tui/app.py` (2 sites: `compose` and `on_mount`), and `tui/commands.py` (1 site: `_cmd_model`) to pass `project_path=project_path` directly. Removed the now-redundant `config.project_path = project_path` lines. The `config.plan_mode = plan_mode` line in `_run_task` was untouched. 117 tests pass.

- **2026-06-13 Code Cleanup**: Unused fixtures and imports removed from `tests/test_project_config.py`.
  **Cause**: Test `test_load_soul_returns_none_when_absent()` declared unused `monkeypatch` fixture; file had unused `import textwrap`.
  **Fix**: Removed `monkeypatch` parameter from function signature and deleted the unused import line. All 5 tests in the file pass.

- **2026-06-13 Feature**: `AgentLoop` now resolves system prompt and soul per-project rather than globally.
  **Cause**: Task 5 of the per-project config work. `DEFAULT_SYSTEM_PROMPT` was a module-level constant loaded once at import time from the dagi root; `dagi_soul` was always read from `dagi_root / "soul.md"` (which no longer exists at that path). Both were oblivious to `config.project_path`.
  **Fix**: Removed `DEFAULT_SYSTEM_PROMPT` constant. `AgentConfig.system_prompt` now defaults to `""`. In `AgentLoop.__init__`, when `config.system_prompt` is empty, `load_main_system_prompt(dagi_root, config.project_path)` is called (project-local first, dagi root fallback). Soul loading replaced direct file read with `load_soul(dagi_root, config.project_path)` (returns `None` when absent). `system_parts` SOUL entry now gates on `soul_text` truthiness rather than file existence. `SessionTracker` now receives `logs_dir=config.project_path / ".dagi" / "logs"` so logs are scoped to the active project. 117 tests pass; continuation tests pass `system_prompt="You are a test agent."` explicitly and take the non-empty branch.

- **2026-06-13 Bug**: `_rebuild_for_normal_mode` and `_rebuild_for_plan_mode` in `agent/loop.py` directly called `self.config.system_prompt.format_map(...)` without the lazy-load guard applied in `__init__`.
  **Cause**: Both rebuild methods (called on tool output that triggers mode changes, e.g. `enter_plan_mode` tool call) called the empty default system prompt directly if `AgentConfig.system_prompt` was unset, producing an empty system prompt instead of loading from disk.
  **Fix**: Added the same lazy-load guard as in `__init__`: assign `_prompt_text = (self.config.system_prompt if self.config.system_prompt else load_main_system_prompt(dagi_root, self.config.project_path))` before calling `format_map()`. Applied to both methods (lines 772–778 and 830–836). All 117 tests pass.

- **2026-06-13 Bug (Fix A)**: Harbor `DagiAgent.run()` set `config.project_path = Path(self.logs_dir)` — Harbor's empty log directory. The agent's `find("**/*")` returned only `.dagi/plans/` DAGI-internal files, misleading the model into thinking those were the task files. The system prompt also emitted `"Project root: <logs_dir>"` directing the model to explore the wrong path.
  **Fix**: Changed to `config.project_path = Path(tempfile.mkdtemp())` in `benchmarks/harbor/agent.py`. The temp dir is empty, so file tools return nothing — nudging the agent toward `harbor_bash`.

- **2026-06-13 Bug (Fix B)**: No Harbor environment context in the system prompt — the model defaulted to `enter_plan_mode` (which strips `harbor_bash` from the tool registry) and used Windows file tools to try to reach container paths (`/app/doomgeneric_mips`). Result: reward 0.0 on every task even for trivial ones.
  **Fix**: Added `system_prompt_preamble: str = ""` field to `AgentConfig` (parsed from top-level yaml). Injected first in `preamble_parts` in `AgentLoop.__init__`, `_rebuild_for_normal_mode`, and `_rebuild_for_plan_mode`. `config_benchmark.yaml` sets the preamble to instructions: use `harbor_bash` for all container access, first action is `harbor_bash("ls /app")`, never call `enter_plan_mode`.

- **2026-06-13 Task 6**: Added `logs/` directory to `/init` command.
  **Cause**: Session initialization did not create a dedicated logs directory; `.dagi/logs` was referenced elsewhere but not pre-created.
  **Fix**: Modified `_cmd_init()` in `cli.py` (line 591) to include `dagi_dir / "logs"` in the directory creation loop. Verification test confirms the directory exists and is a proper directory. All 117 tests pass.

- **2026-06-13 Task 5**: Created `config_benchmark.yaml` with Harbor environment preamble and tool allowlist.
  **Cause**: `benchmarks/harbor/agent.py` and `benchmarks/terminal_bench/agent.py` referenced a non-existent `config_benchmark.yaml` file. Benchmark runs need separate config from production: distinct model catalog, explicit tool allowlist (harbor_bash + file tools), higher `max_continuations`, and a system prompt preamble instructing agents to use bash for all file I/O (Docker container environment).
  **Fix**: Created `config_benchmark.yaml` at project root with:
    - `max_continuations: 30` (higher budget for time-boxed benchmark tasks)
    - `system_prompt_preamble:` instructing use of `harbor_bash` for container file I/O, not host file tools
    - `tools:` allowlist = `[harbor_bash, read, find, grep, write, edit, ask_user]` (host tools for local tasks, harbor_bash for container commands)
    - Model catalog with Claude Sonnet and Opus via OpenRouter
    - `api_error_retries: 5` and `null_response_retries: 5` (same resilience as main config)
    - `bash_backend: subprocess` (no-op, kept for backwards compatibility)
  Verified: config parses correctly, preamble loads with 14 lines and correct first line, `harbor_bash` survives `filter_to` check. Commit: `5be33dd`.

- **2026-06-17 Bug**: `/compact` TUI command caused a crash.
  **Cause**: `_cmd_compact()` in `tui/commands.py` called `self._active_loop.compact_tool.compact(force=True)` directly on Textual's main event loop thread. `compact()` makes a synchronous blocking `client.chat.completions.create()` API call (line 222 of `tools/compact.py`), freezing Textual's event loop entirely. A second issue: `compact()` re-raises any exception, and `_cmd_compact()` had no `try/except`, so API errors (network timeout, rate limit, auth failure) propagated as unhandled exceptions through the Textual event handler, crashing the app.
  **Fix**: Moved the compact operation to a background daemon thread. The main-thread handler now shows an immediate "⏳ Compacting context…" message and returns. The thread calls `compact()` and uses `call_from_thread` to post the result or error message back to the UI. Added `import threading` to `tui/commands.py`. The loop reference is captured before threading to avoid a race if `_active_loop` changes.

- **2026-06-17 Bug (A1)**: TUI sessions never produced a `session_end` JSONL record — token totals, cost, and subagent rollups were silently dropped every session.
  **Cause**: `_agent_work()` in `tui/app.py` (finally block, lines 159–161) only set status to "idle" and re-enabled input. `loop.finish()` (which calls `SessionTracker.finish()`) was never called. The CLI calls it at 6 separate sites; the TUI had zero.
  **Fix**: Added `if loop_ref: loop_ref[0].finish()` in the `_agent_work` finally block. `if loop_ref:` guards against `AgentLoop.__init__` raising before `loop_ref.append(loop)`. The call is wrapped in `try/except Exception: pass` so an `OSError` (disk full, permissions) in `_write()` cannot mask the original agent exception from `loop.run()`.

- **2026-06-14 Refactor**: Moved `config_benchmark.yaml` from project root to `benchmarks/config_benchmark.yaml`.
  **Cause**: Benchmark config is benchmark-specific and belongs alongside the benchmark code, not at the project root where it could be confused with production config.
  **Fix**: Moved file; updated `benchmarks/harbor/agent.py` line 65 (`Path(__file__).parent.parent.parent` → `Path(__file__).parent.parent`); updated `docs/terminal-bench.md`, `README.md`, `TODO.md`, and `PROJECT_CONTEXT.md` to reference the new path.

- **2026-06-18 Bug**: `AttributeError: 'AgentLoop' object has no attribute 'config'` on startup after commit `6591652`.
  **Cause**: The `_build_preamble` refactor extracted the inline preamble block into a method that accesses `self.config`, but `self.config = config` was assigned 25 lines *after* the `_build_preamble(dagi_root)` call in `__init__`. The same commit also deleted the inline block that defined `soul_text`, `dagi_agents`, and `project_agents` — but the `system_parts` construction block at lines 290–295 still referenced all three, producing a latent `NameError` that would have fired after the `AttributeError` was fixed.
  **Fix**: Moved `self.config = config` to immediately before the `_build_preamble` call. Added `soul_text = load_soul(...)`, `dagi_agents = ...`, `project_agents = ...` definitions before the `system_parts` block in `__init__`. All 126 tests pass.

- **2026-06-21 Bug (A1)**: Compaction failure crashed the entire session.
  **Cause**: `CompactTool.compact()` correctly rolls back `_messages` via snapshot on failure, then re-raises. `_compact_context()` had no exception handling, so the exception propagated to the main loop's outer `except Exception as e:` block — which called `on_error(e)` and then re-raised, killing the session. A transient rate-limit on the summarization API call (most likely precisely when context is full and the provider is under load) was therefore fatal.
  **Fix**: Wrapped `compact_tool.compact()` in `_compact_context()` with try/except. Failure emits `f"[Warning: context compaction failed — {exc}. Continuing with full context.]"` via `callbacks.on_assistant_text` and returns `_NO_COMPACTION`. Added `_NO_COMPACTION` to the import in `agent/loop.py`. The session continues; if context is truly too large the next API call will fail with a context-length error handled by the existing retry logic.

- **2026-06-21 Bug**: Worker and review subagents never spawned during plan-work-review execution — the main agent implemented all subtasks directly.
  **Cause**: The plan-work-review skill's Phase 2 instructions (spawn worker → spawn review → evaluate) were loaded as a `skill` tool result early in the conversation. By the time Phase 2 began (after exploration, clarification, grilling, plan writing, and approval), context compaction had summarized these instructions away. The Active Plan section in `_messages[0]` also told the agent to "implement each step" directly, competing with the now-absent skill instructions. The explore_files subagent timing out (300s default) further biased the LLM against spawning subagents.
  **Fix**: (1) Added `## Execution Protocol` section to the plan template in `.dagi/skills/plan-work-review/SKILL.md` — this section is written by the agent into `plan.md` on disk, making it compaction-immune. (2) Rewrote the Active Plan injection in `agent/loop.py:_rebuild_for_normal_mode` to instruct the agent to read the plan file before implementing and to follow the Execution Protocol section exactly.

- **2026-06-21 Bug (A3)**: After a plan→default tier switch, `provider_order` leaked — the default tier kept the plan tier's OpenRouter routing instead of its own.
  **Cause**: `_base_config_snapshot` captured only 5 fields (`model`, `base_url`, `api_key`, `thinking`, `display_name`). `provider_order` was never snapshotted, so the "default" restore path (`target == "default"`) never reset it. The plan/worker apply path likewise never copied `tier_cfg.provider_order` into `self.config`, meaning switching *to* a tier that has `provider_order` set also silently kept the previous tier's value.
  **Fix**: Added `provider_order` as the 6th field in `_base_config_snapshot` (init). Added `self.config.provider_order = snap["provider_order"]` to the default-restore branch. Added `self.config.provider_order = tier_cfg.provider_order` to the `tier_cfg is not None` apply block. Updated the nearby comment from "five" to "six". All three sites are in `agent/loop.py`.

- **2026-06-17 Bug (A2)**: soul.md and both agents.md files were dropped from the system prompt after any plan-mode transition.
  **Cause**: `_rebuild_for_normal_mode` and `_rebuild_for_plan_mode` in `agent/loop.py` each contained only a single-line preamble injection: `if self.config.system_prompt_preamble: new_system = preamble + "\n\n---\n\n" + new_system`. They never loaded `soul.md` or `agents.md`. Only `__init__` assembled the full preamble stack. The bug was introduced when `system_prompt_preamble` was wired to all 3 build sites (commit `fa63170`) but the soul/agents.md loading was never added to the two rebuild methods.
  **Fix**: Extracted preamble assembly into `AgentLoop._build_preamble(dagi_root: Path) -> str`. The method replicates the full `__init__` assembly: system_prompt_preamble → soul.md (project-first, dagi-root fallback) → dagi agents.md → project agents.md, joined with `\n\n---\n\n`. Replaced the 17-line inline block in `__init__` and the two single-line injections in both rebuild methods with `preamble = self._build_preamble(dagi_root)`.
