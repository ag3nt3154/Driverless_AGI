name: dagi
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
      # Run conda env create from the repository root.
      # Core only; add UI/tool groups from requirements-*.txt as needed.
      - -r requirements-core.txt
